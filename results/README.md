# 实验结果

01/02/03 分别对应量化与 I/O、共享图检索、完整磁盘系统。每层按 `<dataset>/<run-id>/` 组织，包含 `manifest.json`、`raw/`、`tables/`、`figures/`，新运行另有调参 `manifests/`。

公共运行记录在 `manifests/`，诊断在 `diagnostics/`，旧结果在 `archive/`。历史导入记录未重新验收，不能因迁移而视为正式结果。图和索引在 `../artifacts/`。

完整规则见 [RESULTS_LAYOUT](../docs/RESULTS_LAYOUT.md)。
