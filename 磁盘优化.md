# Ours 磁盘布局与后续优化方案

更新：2026-09-17。本文先说明已经从代码核实的实现，再给出尚待实现、验证的优化计划。代码范围是当前 05B/05C Ours-Disk 的 DB1 主线；普通布局和 locality 分支分别描述，不把内存对照、adaptive route 或其他消融当成默认路径。

实验内存规则按用户最新决定：**Ours 主实验上限为 2 GiB，baseline 使用各自配置，不要求相同内存预算。** 本文的驻留与缓存优化在 Ours 自身预算内讨论；比较时同时报告各方法实测内存与匹配 Recall 的性能，每个配置只跑一次。

## 1. 我们要解决什么问题

**在固定总 DRAM 预算下，根据量化不确定性和新增页成本，决定哪些数据值得驻留、哪些候选值得提高精度，并在匹配 Recall@10 时减少实际 SSD I/O。**

先验证其中最小的一部分：

- **B：自适应 residual 精化。** 有希望改变结果的候选才读取 residual，不再无差别精化固定数量的候选。
- **C：按页组织精化。** 合并同页候选，利用已经读入的 residual 页，在保证条件不变的前提下调整处理顺序。

第一轮保留现有图、DB1 筛选、fine4 导航、驻留策略与 locality 布局。B/C 通过后，再研究物理布局、异步流水线及自动内存分配。以下方案没有据此修改搜索算法，也没有预设性能收益。

## 2. 当前布局：数据到底在哪里

### 2.1 用一句话理解当前查询

```text
DRAM 中的 DB1 筛掉部分邻居
    → 从 SSD 读取幸存邻居的 fine4，决定下一步扩展谁
    → 搜索结束后，对选出的候选读取 residual，改善结果排序
```

这里的 residual 是量化残差，**不是 FP32 原向量，也不是 exact distance**。

### 2.2 两种磁盘布局

| 数据 | 普通布局 | locality 布局 | 查询用途 |
|---|---|---|---|
| 全库 DB1 / route 位面与 factors | sidecar 文件加载后全量驻留 DRAM | 相同 | 给新邻居做廉价筛选 |
| 邻接表 | `shared_graph.pages` | `graph_compact.pages` | 得到节点的邻居 ID |
| fine4 / compact | `ours_full4_residual.pages` | `graph_compact.pages` | 导航候选距离与排序 |
| 量化 residual | 与 compact 存在同一记录 | 独立 `residual.pages` | 最终结果精化 |
| 逻辑 ID 到物理 slot | 直接按 ID 计算 | `id_to_slot.u32` 加载到 DRAM | 定位重排后的记录 |
| 数据库 FP32 原向量 | 不在该查询记录中 | 不在该查询记录中 | 建索引/导出时作为输入 |

```text
普通布局
  DRAM：全量 DB1 + factors + 模型 + 查询状态/缓存
  SSD graph：  [degree | neighbor IDs] × 多个节点
  SSD payload：[compact | residual] × 多个节点

locality 布局
  DRAM：上述对象 + ID→slot 映射
  SSD combined：[degree | neighbor IDs | compact] × 多个节点
  SSD residual：[residual] × 多个节点
```

普通布局在为 fine4 读 payload 页时，已经把页内 residual 字节一起读进来。locality 才把 residual 的物理读取推迟到精化阶段。**后续 B/C 首先在 locality 上验证。**

### 2.3 对七个问题的直接回答

| 问题 | 当前代码的答案 |
|---|---|
| 从 `u` 怎样得到 `N(u)`？ | 普通路径调用 `graph.read_nodes(&[u])`；locality 调用 `locality.neighbors(u)`。两者都解析 `degree + u32 neighbor IDs`。 |
| route 按什么组织？ | DB1 按内部 node ID 连续存放：`msb_base + id × stride`，factors 也是逐节点数组。不是父节点/边局部副本。 |
| route 在 DRAM 还是 SSD？ | `ours_db1_sidecar.bin` 是持久化文件，加载时整份读入 DRAM，查询 gate 不按邻居随机读取 sidecar。SSD fine4 中仍包含最高位信息。 |
| FP32 原向量在哪？ | 导出阶段读取 `<data-root>/<dataset>/<dataset>_base.fvecs`。当前磁盘查询不读取数据库 FP32 做导航或重排；浮点 query 和共享浮点模型仍存在。 |
| fine/residual 在哪？ | 见上表。locality 将 graph+compact 共置，将 residual 分离。 |
| 一次 expand 读什么？ | 先取 `u` 的邻接页，再取通过 DB1 gate 的新邻居的 compact 所在页。实际 SSD 请求取决于缓存、页去重和批次。 |
| cache 的单位是什么？ | 完整 4 KiB page，键为 `(file_id, page_id)`，不是固定的整条 node row。 |

C++ `VamanaIndex` 内部用 `graph_` 邻接数组表示图，但磁盘运行时通过 page reader 取邻接表，不能因此说磁盘搜索整张图驻留 DRAM。adaptive route 是另一个分支：加载独立 route 数据并不加载完整 DB1 数组，不是现有主线自动执行的混合驻留策略。

### 2.4 一次 `expand(u)` 的真实顺序

```text
从 fine4 候选池取出 u
  1. 取得 u 的邻接表（页面可能命中缓存）
  2. 去掉已 visited 的邻居
  3. 用 DRAM DB1 + factors 筛选，得到幸存集合 S
  4. 分批读取 S 中节点的 compact 所在页
  5. 计算 fine4 距离，更新导航候选池
```

普通布局第 1 步访问 graph 文件，第 4 步访问 compact+residual 文件。locality 两步都访问 combined 文件：给邻居 v 算 fine4 时，其邻接表也随页进入缓存；以后扩展 v 时，如果该页还在缓存，就可复用。

locality 普通扩展不主动读独立 residual 页。当前最终重排先取得所选候选的 compact，再取得 residual，使用浮点 query 计算修正后的距离估计。

令 combined 记录长为 `Sg + S4`，一页容纳 `ng=floor(4096/(Sg+S4))` 条，则：

```text
combined_page(v) = floor(slot(v) / ng)
residual_page(v) = floor(slot(v) / nr)
nr = floor(4096 / residual_record_bytes)
```

两类文件各自按记录长度打包；即使使用同一个 slot 映射，同一节点的两类页号也不一定相同。一次 expand 涉及 `u` 和 S 对应的页面，**不是固定一次 I/O**。不同批次之间如果发生淘汰，同一页还可能被重复读取。

### 2.5 缓存、记录大小与现有能力

- graph、payload 或 locality reader 共享一份有界查询缓存；默认预算为每查询 4 MiB，包含页面槽位和哈希/LRU 元数据，不是每个文件各获 4 MiB。
- 查询缓存在每个 query 开始时重置。普通 graph reader 另有可选共享图页缓存，需独立计账，不能把两种生命周期混为一谈。
- 解码后的邻居数组、gather 数组、I/O 返回缓冲属于临时工作区，不能只统计 page cache 就认为工作区已全部计入。
- 已实现批次内页去重、相邻页合并及底层 AIO；当前调用会等待批次读完，不能等同于完整的跨批次计算/I/O 流水线。

以当前普通 residual4 格式、最大图度数 64 为例：

| 原始维度 D | 编码维度 d | DB1 位面/节点，不含 factors | graph+compact/节点 | combined 每页节点数 | residual/节点 |
|---:|---:|---:|---:|---:|---:|
| 960 | 1024 | 128 B | 789 B | 5 | 648 B |
| 1024 | 1024 | 128 B | 789 B | 5 | 648 B |
| 1536 | 2048 | 256 B | 1301 B | 3 | 1288 B |

估算依据：graph=`4(R+1)`，compact=`d/2+17`，residual=`5d/8+8`；编码维度会补齐，其他格式/参数必须重算。DB1 还要加 factors，不能无条件声称它比经典 32 B PQ 更小。locality v1 要求 combined 和 residual 的单条记录各自不超过 4096 B。

## 3. 为什么先做 B/C

当前问题不是“导航误读了整份 FP32”，因为这条查询路径本来就不读取数据库 FP32。也不是“没有缓存或页去重”，因为这些已经存在。

真正需要回答的是：

1. 固定数量 residual 重排中，有多少候选不可能改变最终结果？
2. 少精化一些候选，能否真正少读页面，而不只是少算距离？
3. 利用同页候选和 residual 缓存，能否更快取得足够好的结果阈值？
4. 保存判定所需元数据、重算距离和维护队列的开销，会不会超过节省的 I/O？

B/C 的目标是隔离这四个问题。第一版仍在导航结束后精化；搜索过程中的在线精化不是首轮前提。

## 4. 阶段 A：固定基线，补齐测量

### 4.1 固定比较对象

保存图、编码、布局、输入、源码与二进制哈希，明确查询线程数、缓存生命周期和总预算。

用 `Cq` 表示某个 query 导航结束后的完整 fine4 候选池；用 `Rrefine` 表示旧逻辑的重排数量，避免和图度数 R 混淆。旧实现实际重排数量为 `min(|Cq|, max(Rrefine,k))`。

| 版本 | 导航路径 | 精化策略 | 用途 |
|---|---|---|---|
| A0 | 当前 DB1 + fine4 | 当前固定数量 residual | 现有系统的实际对照 |
| A1 | 与 A0 相同 | 对 Cq 全部计算 residual | 集合内正确性参照 |
| B | 与 A0 相同 | 区间驱动自适应精化 | 判断不确定性筛选是否有效 |
| C | 与 A0 相同 | B + 页分组/缓存优先 | 判断页成本调度是否有效 |

A1 可能比 A0 精化更多节点，结果也可能不同。B/C 首先对齐 A1 的集合内结果，但系统收益必须与 A0 比较，不能只证明比更昂贵的 A1 快。

### 4.2 分开记录计算、缓存和实际读取

至少记录：

- Cq 大小、residual 候选数、实际精化数、区间排除数。
- compact 和 residual 各自请求的不同页数、实际读取页数、字节数、提交批次数。
- 各类页面命中/未命中/淘汰；重复读取与批次内去重数量。
- 浮点 fine4 重算次数及耗时、区间构造耗时、residual 计算耗时。
- 整个 query 的延迟、QPS、P95、Recall@10 和进程内存峰值。

不同页数不是实际读次数；提交次数不是页数。当前 reader 的综合耗时可能包含查缓存、复制和解码，应与底层 I/O 等待区分。所有新增开销都纳入端到端计时。

## 5. 阶段 B：根据不确定性决定是否读 residual

### 5.1 保证对象：当前 residual 分数，不是全库真实距离

首版只要求在固定 Cq 内，返回与“全部计算 residual”相同的有序 top-k。它不修复导航没访问到的近邻，也不承诺 FP32 exact。

每个候选维护：

```text
CandidateRefinement
  node_id
  float_fine4_score
  lower_bound / upper_bound
  residual_page_id
  state: pending / refined / pruned
  refined_score（精化完成后）
```

导航池继续按 fine4 维护。不能用 residual 结果边界直接删除导航桥接节点。

### 5.2 区间怎样得到

按当前估计器的理想代数，令 y 为浮点旋转 query，ehat_u 为实际解码的量化残差：

\[
s_{res}(u)=s_4^{FP}(u)-2y^\top\widehat e_u,
\qquad
|s_{res}(u)-s_4^{FP}(u)|\le 2\|y\|\|\widehat e_u\|.
\]

离线保存 `||ehat_u||` 的保守上界 a_u，查询时构造：

\[
\Delta_u=2\|y\|a_u+\varepsilon_{num}(u),\qquad
[L_u,U_u]=[s_4^{FP}(u)-\Delta_u,s_4^{FP}(u)+\Delta_u].
\]

`epsilon_num` 必须覆盖实际计算路径、存储精度和浮点舍入误差，不能随意填一个经验小常数就称为保证。以上是待实现并推导验证的方案，不是已确认可直接复用的现有误差界。

实施顺序：

1. 核对当前 residual 内核各配置分支，固定一个评分端点作为首版。
2. 离线从实际保存、实际解码的 residual 计算范数上界；核对现有 factor 的定义，不能仅凭名称复用。
3. 首版用紧凑驻留元数据提供上界；若先读取整个 residual 页才能获得上界，就没有避开那次 I/O。
4. 当前导航可能使用 INT8 query，而 residual 路径使用浮点 query 重算 fine4。先为 Cq 重算正确的浮点基值，并记录 compact 读取和计算成本；将来再考虑对端点差异建立额外界。
5. 保守保存上界，并验证实际数值内核不会越界。无可靠界时将候选标为必须精化；非有限精化结果沿用明确的异常策略，不静默忽略。

新增范数元数据按 N 计费，例如每节点 4 B 就是 4N B，不能免费叠加到固定预算之上。若保守界过宽，可研究分块界，但先衡量其更多元数据和计算成本。

### 5.3 自适应精化流程

```text
输入：固定候选池 Cq，目标 k
为候选准备分数区间，按 (lower_bound, node_id) 排序
维护已精化结果的 top-k

while 仍有 pending 候选：
    若已有 k 个结果，令 tau 为当前第 k 名分数
    若剩余所有 lower_bound > tau：结束
    选择最有竞争力的 pending 候选
    取得 compact/residual，调用相同的 residual 评分内核
    更新结果 top-k 与候选状态

返回与参照一致的距离排序及 ID tie-break
```

边界条件：

- 结果不足 k 时不能用未形成的阈值排除；如果 `|Cq|<k`，处理全部候选并返回实际数量。
- `L_u=τ` 时保留候选，除非已有经过验证的 tie-break 判定。
- 不能排除时继续精化，最坏情况退化为 A1。
- 若设置时间/I/O 上限而提前停止，须另标近似版本，不能声称集合内结果等价。
- 使用统计区间的版本另做消融并报告漏排率，不与保守区间混写。

## 6. 阶段 C：根据新增页成本安排精化顺序

### 6.1 先分组，再决定读取

将 B 的 pending 候选按 `(file_id,page_id)` 分组。页内保留候选 ID、区间、已完成状态；页面组维护最小下界和缓存状态。

首版用确定性顺序：

1. 已缓存 residual 页中仍有竞争力的候选优先。
2. 其余页面按组内最小 L_u 排序。
3. 分数相同时按稳定页 ID 排序。

与 B 的单纯下界顺序比较。缓存优先只是待验证策略；如果先处理很差的缓存候选反而延迟收紧 τ，应据消融结果调整，而不是认定命中必然有利。

### 6.2 页到达后如何处理

- 一页因某个候选被读入后，复用它处理 Cq 中同页且仍有竞争力的其他候选。
- 每次结果阈值变化后复查区间，已能排除的候选无需再算。
- 不为了利用整页而自动把 Cq 外的节点加入结果参照；那会改变本轮保证的候选集合。
- 有限批量读取可以合并同页请求，但批次过大会提前读入随后被排除的页，需要记录无效预读。
- 页命中只免去 residual 的那次读取；compact 若不在内存，仍有独立成本。B 的实现可选择保留必要 compact，但必须计入工作区。
- 图+fine4 的 combined 页与 residual 页是不同文件，前者命中不代表后者可用。
- 冷页候选不能因优先级较低被永久跳过；终止条件仍与 B 相同。

当前 reader 已经会对一次批量调用做页去重。因此 C 的新增价值应是**跨精化决策的分组、动态阈值复查和缓存复用**，而不是把现有批次内去重再次当作新优化。

### 6.3 验证后再尝试更复杂的评分

后续可考虑“预计减少的 top-k 不确定性 / 新增读取与计算成本”。该评分只决定处理顺序，不代替安全排除条件。先用确定性规则得到可解释的对照，再决定是否需要学习模型。

## 7. B/C 怎样验收，什么情况下停止扩展

### 7.1 机制验证

对同一查询固定 Cq，比较 B/C 与 A1 的有序 top-k、距离端点和 tie-break。覆盖区间相等、候选不足 k、无有效界、同页多个候选、缓存淘汰、记录边界及读取失败等情况。测试通过不能替代保守界的数学和数值推导。

除合成边界用例外，在真实验证查询上观察区间宽度、可排除比例及实际节省页数，不只看距离计算减少量。

### 7.2 系统验证

在独立验证集选择配置，冻结后运行测试集。在相同总内存和匹配 Recall@10 下比较 A0、B、C 的完整查询性能。遵循[四方法公平对比方案](docs/plans/DISK_BASELINE_FAIR_COMPARISON_20260916.md)：统一环境、32 查询线程、至少三次测量、记录波动，不修改官方 baseline 算法。

第一轮保持搜索参数一致以隔离机制；正式 matched-recall 比较再允许各版本在验证集选择搜索宽度。新增元数据、浮点基值重算、缓存调整和精化调度全部计入。

成功证据应同时回答：是否少读实际页面/字节、recall 是否匹配、整体 QPS/P95 如何变化、总内存是否满足限制。不能只因 B/C 比 A1 快，或 residual 计算次数变少，就宣布优于现有 A0。

### 7.3 暂停条件

| 现象 | 先处理的问题 |
|---|---|
| 几乎所有候选都需要精化 | 分析区间过宽的来源；评估更紧界的成本 |
| 少精化但读取页数不降 | 检查候选同页分布和现有批处理已实现的复用 |
| residual I/O 下降但整体变慢 | 检查 compact 重读、浮点重算和队列管理 |
| 新元数据压缩了缓存，导致导航读量上升 | 在完整预算下重新评价，不只看精化阶段 |
| 批量调度读入很多后来无用的页 | 缩小批次，统计阈值变化与无效读取 |

B/C 无明确收益时，先解释原因，不直接叠加导航图、学习调度或动态驻留来掩盖问题。

## 8. B/C 之后，按依赖逐步扩大系统

### 8.1 精度分层与页面重排

先沿用 graph+compact / residual 分离。分别统计导航共同访问和 residual 共同精化关系，再比较 BFS 与更适合的页面排列。访问频率高不等于总是共同访问，不能只按单点热度排序。

独立 residual 映射只有在收益足以覆盖额外映射内存时才引入。低三位存储可以作为单独消融：由 DRAM 最高位与 SSD 低三位恢复 fine4，必须检查位级一致性、解码成本和每页记录数变化。不要一开始把每个位面拆成独立文件，避免增加串行 I/O。

### 8.2 异步读取与计算重叠

新增 `Absent → InFlight → Resident` 页面状态，以文件和页号去重在途请求。提交下一批已确定读取时，计算上一批已完成数据；限制在途页数和双缓冲容量，并计入所有线程内存。

第一版按确定顺序提交候选更新，便于与原结果对照。完成顺序驱动的搜索另作算法变体。以 32 workers 的整体吞吐和尾延迟判断收益，不能只看单 query。

### 8.3 搜索过程中提前精化

B/C 的首版在导航后工作。后续若提前精化，维护独立导航池与结果池，不让 residual 分数无意改变 fine4 导航。

必须重新定义可返回集合：提前精化的节点之后可能被最终导航池淘汰。若要保持原结果语义，最终只从原定义集合返回；若允许所有已精化节点返回，则明确是候选集合扩大的新版本，并重新做 recall/成本对照。

### 8.4 固定总 DRAM 下的自动驻留选择

先实现配置可行性，再做自动选择：

1. 参数化缓存预算并完善实际峰值计账。
2. 实现 Hybrid route：驻留命中直接评分，未驻留按页读取；不得因缺码跳过候选。
3. 如引入小型导航图，计入其向量、边、映射和工作区，与相同预算下增加缓存进行对照。
4. 在独立验证集枚举少量 route 驻留比例、缓存容量、导航图规模，选出满足预算和目标 recall 的低延迟配置。
5. 冻结配置后测测试集。首版离线自动配置，不在查询过程中动态迁移数据。

总预算为：

\[
M_{codes+factors}+M_{bounds}+M_{cache}+M_{nav}
+M_{workspace}+M_{model+mapping}+M_{runtime}\le B.
\]

其中 workspace 覆盖全部线程、I/O 缓冲和临时数组，runtime 包括分配器、线程栈等开销并保留余量。新增对象必须从同一预算中支付。物理内存约束与 `RLIMIT_AS` 分开报告：后者限制虚拟地址空间，不能称为 DRAM 硬限制。

Parent-local route 是更后置的低内存消融：需要复制邻居码，可能显著增大索引和读取。全量 DB1 能合理驻留时，不应为模仿 AiSAQ 而默认采用复制布局。

## 9. 代码改造位置与交付顺序

| 模块 | 当前职责 | 后续改动 |
|---|---|---|
| [ours_port.rs](experiments/05_disk_system_fair/native_rust/src/ours_port.rs) | DB1 gate、fine4 导航、固定数量 rerank | 提取精化接口，增加 A1/B/C 策略、候选状态和统计 |
| [space_rabitq.h](Ours/core/hnswlib/space_rabitq.h) | 编码、主码及 residual 距离内核 | 核对评分端点，导出保守 residual 范数元数据及所需桥接接口 |
| [locality.rs](experiments/05_disk_system_fair/native_rust/src/locality.rs) | ID→slot、combined/residual 页读取 | 暴露页定位、分组读取与分文件统计，后续再改布局 |
| [query_page_cache.hpp](experiments/05_disk_system_fair/native/query_page_cache.hpp) | 4 KiB 页面缓存及 LRU | 提供调度所需命中查询；明确是否改变 LRU/计数，后续参数化预算 |
| [main.rs](experiments/05_disk_system_fair/native_rust/src/main.rs) | 加载、线程工作区、运行与报告 | 策略开关、上界文件版本/哈希、总内存与结果清单 |
| [export_ours_locality_layout.py](scripts/local_runs/export_ours_locality_layout.py) | 导出 locality 页面和映射 | 后续布局消融与一致性检查；B/C 首轮不改物理格式 |

按顺序交付：

1. A0/A1 对照与分层计数，确定 residual 阶段还有多少可节省 I/O。
2. 评分区间的推导、元数据导出及数值验证；给出额外内存成本。
3. B 的固定候选集合自适应精化与正确性验证。
4. C 的页面分组、缓存机会利用及消融。
5. 同总预算、匹配 recall 的端到端报告，明确通过或未通过原因。
6. 根据证据选择后续布局、流水线或驻留配置优化，不同时打开所有改动。

论文贡献应落在“量化不确定性如何指导精度投入，新增页成本如何指导执行，固定总预算如何约束选择”的机制和证据上。页去重、异步 API、分文件存储本身不能代替这个验证。
