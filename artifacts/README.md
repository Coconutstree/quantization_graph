# 可复用实验产物

- `graphs/`：按数据集和方法保存的图与建图记录。
- `indexes/`：历史索引及兼容引用。
- `query_splits/`：共享和按运行生成的查询划分。
- `dataset_manifests/`：已有数据准备记录。

这里存放生成或复用的输入产物，不是论文性能结果；原始数据仍在 `../data/`。大文件默认不纳入 Git，删除后需按原参数重新生成。
