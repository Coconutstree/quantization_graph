# Ours-Disk 内存分配：常驻路由 + 静态热点 + 动态记录

更新：2026-09-23。

**最终方案按访问职责分配内存：路由表示常驻，热点记录预加载，其余记录按需进入动态缓存。** 在整进程预算内先保障搜索所需的固定开销与路由数据，再按显式缓存策略分配余额。`graph_first` 保持图缓存优先；新增 `records_only` 将余额全部交给静态热点和动态记录。默认策略暂不切换。完整路由放不下时，采用可常驻的 PCA 前缀。

## 1. 内存如何分配

一次查询沿图扩展候选：邻接表提供后续访问的节点，1-bit codes 与 factors 用于粗筛，compact 4-bit 记录用于细算，residual 记录用于校正量化误差并完成重排。方案按照这些数据的用途组织内存：

```text
整进程预算
  ├─ 固定开销：worker、旋转矩阵、查询及进程预留等
  ├─ 常驻路由：1-bit codes + factors
  │            完整表示放不下时，选择可常驻的 PCA 前缀
  └─ 剩余空间
       ├─ BFS 图页缓存：先按剩余预算尽量容纳可选图页
       └─ 记录缓存
            ├─ 静态热点：按 validation 排名提前加载、固定保留
            └─ 动态记录：用余额建缓存，查询实际读到后填充
```

路由层服务于搜索前段的反复筛选，优先常驻以避免路由缺页；记录层服务于通过筛选的候选，用静态与动态两部分减少重复读取。

**静态部分利用已知热点。** 只用独立 validation 查询统计记录访问、生成冻结排名。在记录预算内依次加载热点节点的 compact 和 residual，最多装到名单末尾，正式查询期间不淘汰。

**动态部分利用剩余空间。** 静态分配完成后，用余额建立固定容量的 16 分片 FIFO 记录缓存，接纳非静态节点的实际访问。分配包含映射、槽位、有效位和数据缓冲开销；空间不足以容纳元数据和最小槽位时，动态容量为 0。容量覆盖全部非静态节点后不继续超配。

静态与动态没有固定比例，具体容量由预算和热点名单共同决定。这里的 dynamic 指缓存内容随访问更新，内存配额在查询前确定。本文描述正式 `standard` 模式；`c0` 对照模式关闭图页与记录缓存。

## 2. 查询如何使用这些内存

查询使用常驻路由表示筛选候选，需要 compact 或 residual 时按以下路径取数据：

```text
请求某节点的 compact / residual
  → 静态记录命中：直接取用
  → 否则查动态记录：对应部分有效则取用
  → 仍未命中：按原路径读盘，并将实际读到的非静态记录填入动态缓存
  → 针对当前查询计算距离，继续搜索或重排
```

动态缓存按节点 ID 分成 16 个独立分片。分片满时淘汰该分片最早插入的节点及其两部分记录；命中不改变 FIFO 顺序，分片之间不借用空位，静态热点不参与这条淘汰队列。

compact 与 residual 分别标记有效：已缓存 compact 不代表 residual 可用，也不会为了补齐另一部分额外读盘。动态槽位按访问填充，因此预留容量不等于已经装入有效数据。

记录缓存保留的是节点所需数据，磁盘缺失读取仍以页为单位。例如某页包含 A、B、C，本次只取用 B，就可在动态记录缓存中仅保留 B。缓存命中省去对应读取，距离仍需重新计算，图页等其他读取也可能继续发生。

## 3. 预算不足时，PCA 和剩余内存如何衔接

**PCA 的触发条件是“固定开销 + 完整路由”超出预算。** 其中固定开销包括中心向量、worker 预留、原维度旋转、进程和查询预留。这些项不会因采用 PCA 而消失；如果连固定开销本身都放不下，降维也无法解决，规划最终报预算不足。

设整进程预算为 B，选定路由后的必要占用为 R。完整 1-bit 方案满足 R ≤ B 时直接使用；否则按 PCA512 → PCA256 → PCA128（只考虑低于原维度的候选）尝试，将低维 codes、factors、投影矩阵和低维计算预留一并计入 R，选择能完整常驻的最高候选维度。PCA128 仍放不下则拒绝运行，不转为分页、不减少 workers。维度选择细节见第 5 节。

**先冻结表示，随后 full1bit 与 PCA 调用同一个缓存分配入口。以下为默认 `graph_first`：**

```text
可用缓存空间 C = B − R
  → BFS 图页：页数上限 = min(floor(C / 4096), 图布局的节点页数上限)
             从入口按 BFS 选页，实际占用 G = 选中页数 × 4096
  → 记录预算 H = C − G
             先按 validation 排名加载静态热点
             再用余额建立 16 分片 FIFO 动态记录缓存
```

新增 `records_only`：不建立共享 BFS 图页缓存，`H = C = B − R` 全部交给 hot + dynamic；图仍在 SSD 按需读取，查询内缓存不变。两组使用同一表示计划、PCA 资产和 validation 热点排名，不能让缓存策略反过来改变维度。动态容量可能为零，不为保证缓存容量而额外降低维度。

在默认 `graph_first` 中，**BFS 图页优先使用余额，没有给记录缓存预留最低额度**，也没有固定的 32.55 MiB 图缓存上限。图页可能用掉几乎全部 C，此时静态、动态记录容量都可能为 0；有记录空间但静态热点尚未装完时，动态容量也可能为 0。这些都是同一分配规则在不同预算下的结果，不是 PCA 专用策略。

两条路由都使用完整维度的 compact/residual 记录和同一套缓存实现，但命中情况不必相同：PCA 会改变候选访问，validation 热点清单也需绑定对应路由计划，不能直接套用完整路由的旧名单。PCA 只改变前段粗筛，后续完整 4-bit 细算和 residual 重排仍保留。

这里的 R 是代码中的规划与准入账本，不能代替整进程峰值 RSS 验收。实现见 [路由规划](experiments/02_disk_shared_graph/native/src/pca.rs)、[搜索与缓存分配](experiments/02_disk_shared_graph/native/src/main.rs)、[BFS 选页](experiments/02_disk_shared_graph/native/src/lib.rs)、[记录缓存加载](experiments/02_disk_shared_graph/native/src/record_cache.rs)。

## 4. 实验如何支持这个选择

策略证据来自 `results/04_ours_memory_budget/`，以 GIST 为主、AG News 为补充。实验分别检查路由常驻、记录缓存和低预算分支；03/05 仅用于核对落地配置，不与 04 拼接性能曲线。

### 4.1 路由常驻的依据

GIST 分页诊断中，793.2 MiB 整进程预算下，将 factors 从分页改为常驻，QPS 从 **10.27 提升到 16.25**；再固定保护热点 codes 页，QPS 为 15.92。868.2 MiB 档，保护热点页也仅使 QPS 从 41.97 变为 42.34。同期对照 Recall 均为 0.9440。

这表明路由数据的驻留安排影响显著，而固定保护部分热点页的收益不稳定，为优先保障常驻路由提供了依据。[分页诊断数据](results/04_ours_memory_budget/gist_诊断日志/tables/routing_factors_hot_comparison.md)

### 4.2 静态热点与动态记录的整体收益

记录缓存对照包括四种配置：无额外缓存、纯静态 Hot Records、静态热点 + 动态页（Hybrid）、最终采用的静态热点 + 动态记录（Hot + Dynamic）。每行四方案 Recall 相同，QPS 为两轮合并结果。

| 数据集 / L | Recall@10 | 无额外缓存 | Hot Records | Hybrid 37.5% | Hot + Dynamic | 相对 Hybrid |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| GIST / 100 | 0.951375 | 111.40 | 271.18 | 303.42 | **412.92** | +36.1% |
| GIST / 300 | 0.983500 | 50.23 | 123.85 | **200.24** | 188.35 | −5.9% |
| GIST / 580 | 0.986500 | 28.88 | 69.98 | 98.19 | **118.62** | +20.8% |
| AG News / 100 | 0.992625 | 188.66 | 320.48 | 288.20 | **383.32** | +33.0% |
| AG News / 300 | 0.994375 | 90.89 | 153.08 | 200.66 | **205.72** | +2.5% |
| AG News / 580 | 0.994375 | 51.87 | 89.36 | **135.76** | 135.40 | −0.3% |

![GIST 四种缓存方案的 test Recall–QPS 曲线](results/04_ours_memory_budget/dynamic_records/gist/figures/recall_qps.png)

GIST L100 中，纯静态热点已将 QPS 从 111.40 提升到 271.18，读页从 1100.38 降到 350.61 页/查询，但仅使用 402.69 MiB 的 1189.79 MiB 新增缓存额度。最终方案沿用同一静态集合，并用原先未使用的余额补充动态记录，在表中六个代表点均超过纯静态热点。

与 Hybrid 相比，最终方案在两个数据集 L100 的 QPS 分别高 36.1% 和 33.0%，缓存计账也更少：GIST 为 **1133.18 vs 1189.76 MiB**，AG News 为 **871.67 vs 1267.56 MiB**。但 GIST L300 回退 5.9%，AG News L580 基本持平，因此结果支持当前默认选择，并非逐档最优。

### 4.3 Hybrid 对照说明什么

Hybrid 与最终方案都采用“静态热点 + 动态补充”，区别在具体组织方式：

| 对比项 | Hybrid | 最终方案：Hot + Dynamic |
| --- | --- | --- |
| 动态缓存单位 | 4 KiB 整页 | 非静态节点的 compact/residual 记录 |
| 分配规则 | 热点邻接表上限 25%，静态记录上限选为 37.5%，实际余额给动态页 | 图缓存另行安排，记录预算内先装热点，再将余额给动态记录 |
| 替换方式 | 16 分片 FIFO，淘汰页 | 16 分片 FIFO，淘汰节点记录 |
| 复用范围 | 同页其他数据也可受益 | 直接保留实际取用的记录 |

Hybrid 的 37.5% 由两个数据集各自的 validation 六档扫描选出，并在 test 前锁定；它是对照方案的静态记录上限，不是最终方案的静态/动态比例。[GIST 配额报告](results/04_ours_memory_budget/hybrid_微调/report.md) · [AG News 配额报告](results/04_ours_memory_budget/hybrid_agnews/report.md)

**这项对照比较两套完整配置，不能将差异单独归因于“页改成记录”。** 粒度、配额和邻接表缓存安排同时变化。GIST L100 中，Hybrid 读页更少（137.18 vs 244.44 页/查询），吞吐却更低，也说明读页数不足以单独解释性能。

### 4.4 低预算分支的依据

以下是 04 的固定 128 维 PCA 消融，用于验证低预算路由的取舍；其维度与 12/8 MiB 记录缓存配置不是当前自适应规划器的输出。GIST 在 538 MiB 整进程预算下，固定 M=32、相同图，validation 选定 width 后进行 test800 两轮确认：

| 路由方案 | 路由维度 | width | Recall@10 | QPS | 路由页/查询 | 记录缓存 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| PCA + 1-bit | 128 | 100 | 0.951375 | **40.80** | **0** | 12 MiB |
| PCA + 1-bit + residual norm | 128 | 100 | 0.951375 | 40.49 | **0** | 8 MiB |
| 完整 1-bit 分页 | 960 | 100 | 0.951625 | 21.55 | 3620.63 | 0 |

PCA 方案接受更多候选细算，换取消除路由缺页：相对完整 1-bit 分页，Recall 低 0.025 个百分点，QPS 高 89.3%。这支持低预算下的取舍，并不说明 PCA 优于完整维度常驻方案。

PCA 尾部范数额外占用 3.815 MiB，挤压记录缓存却未提高 Recall，因此默认不加。等缓存的 validation 对照没有显示范数计算本身拖慢系统；结论仅是本轮固定总预算下未见净收益。M=32 为固定实验条件，未在本轮优化。[低预算消融报告](results/04_ours_memory_budget/gist_pca_residual_ablation/report.md)

### 4.5 证据边界

缓存对照采用相同额度上限，但实际占用不同；旧三方案复用历史结果，最终方案使用新二进制测量，存在时段与二进制混杂。每档动态缓存先清空、再预热 100 条，正式计时包含缓存维护与 I/O；曲线阴影仅为两轮最小—最大值。[实验方法与结果](results/04_ours_memory_budget/dynamic_records/report.md) · [GIST CSV](results/04_ours_memory_budget/dynamic_records/gist/comparison.csv) · [AG News CSV](results/04_ours_memory_budget/dynamic_records/agnews/comparison.csv)

现有证据用于支持默认配置。若要分离缓存粒度的贡献，需要固定路由、图缓存、静态热点集合与动态字节预算，在同一二进制中仅切换动态页/记录并交错复测。纯动态记录、紧容量淘汰与分布漂移也尚缺完整对照，不能据此声称热点优先普遍最优。

## 5. PCA 如何按内存预算自适应

### 5.1 自适应的是路由维度

当前实现是**查询运行前的预算自适应**：输入节点数 N、原维度 D、worker 数、查询预留和预算 B，输出路由模式与维度 k。完整路由能放下就直接使用；否则按 k = 512、256、128（跳过不低于 D 的候选）逐个检查，返回第一个满足预算的候选。128 仍放不下则明确不支持该预算。这个决定在两种缓存策略之前冻结。

选择依据是内存账本，不是累计解释方差阈值、validation QPS 或目标 Recall。代码虽记录保留方差比例，但不据此决定 k；M=32、epsilon=1.9 和不加 PCA 尾部范数也均为固定设置。查询期间不改变 k，也不重新拟合 PCA。

### 5.2 每个候选维度怎样计账

令 P(k) 为 `max(k, 64)` 向上取到 2 的幂，F 为第 3 节中不随 k 缩减的固定开销。当前 `choose()` 使用以下字节账本：

| 项目 | 完整路由（k=D） | PCA 路由（k<D） |
| --- | --- | --- |
| 固定开销 | F | F |
| 1-bit codes | N × P(D) / 8 | N × P(k) / 8 |
| factors | 20N | 20N |
| 均值与投影矩阵 | 0（无额外 PCA 项） | 4 × (Dk + D) |
| PCA 旋转预留 | 0 | 16 × P(k)² |
| PCA 查询临时空间 | 0 | 16 × workers × D |

这些项相加得到 R(k)，以 R(k) ≤ B 判断是否能放下。PCA 压缩的是路由 codes，factors 仍按每节点 20 字节计账，原维度固定开销也仍保留，还要支付新增投影开销。

实际存储维度有补齐台阶，当前 PCA 候选固定为 512/256/128。因此可用维度和缓存余额不会随预算平滑变化，不再尝试 129 或低于 128 的 PCA 维度。完整路由不支付 PCA 附加开销，代码会先单独检查它。

### 5.3 PCA 如何准备并用于查询

离线准备只从 base 数据中最多抽取 32,768 条向量，以固定种子拟合均值和 PCA 基，按特征值从大到小排序。选定 k 后取前 k 个主成分，将全库投影并生成对应的 1-bit codes 与 factors；预算变化时可以复用已拟合的基，按新 k 准备资产。

运行时对查询使用同一均值和投影矩阵。每次图扩展先按低维分数取最多 32 个候选，再使用 epsilon=1.9 的经验门控，保留下来的候选继续完整维度 4-bit 细算和 residual 重排。这个门控不构成原空间距离的安全剪枝保证。

validation 和 test 按同一预算账本规划；查询预留使用未拆分的 query/groundtruth 文件，避免因拆分大小不同选择不同 k。test 校验路由计划、PCA 资产与热点清单哈希，并复用 validation 冻结配置。实现见 [PCA 准备流程](src/disk_bench/ours_pca.py)、[查询路径](experiments/02_disk_shared_graph/native/src/ours_port.rs)。

### 5.4 当前自适应的边界

**当前目标是“路由维度尽可能高且能常驻”，然后将余额交给第 3 节的统一缓存流程。** 规划器不会主动降低 k 来换更多图页或记录缓存，也不会在完整路由已能放下时继续比较 PCA 的吞吐。

因此，启用 PCA 不保证有动态记录缓存，也不等于找到了 QPS 最优的维度与缓存组合。要验证联合最优，需要额外比较“不同 k + 对应缓存空间”的端到端结果；这不属于当前代码已经实现的自适应。

## 附：预算与实现核对


以下为此前保存的规划/运行快照，展示预算量级；图页与记录容量不作为当前代码的固定配置，重跑应按第 3 节计算并核对实际账本。

GIST、N=1,000,000、D=960、32 workers 的规划快照中，固定项与完整路由合计 **611.78 MiB**，其中 worker 预留 414.52 MiB，codes + factors 为 141.14 MiB。因此，“完整路由能否放下”要看整进程账本，不能只看 codes 的 122.07 MiB。

| 整进程预算 | 固定项 + 完整路由 | 图页缓存 | 记录缓存预算 | 记录缓存计账 |
| ---: | ---: | ---: | ---: | ---: |
| 1 GiB | 611.8 MiB | 32.55 MiB | 379.7 MiB | 379.7 MiB |
| 2 GiB | 611.8 MiB | 32.55 MiB | 1403.7 MiB | 1133.2 MiB |
| 4 GiB | 611.8 MiB | 32.55 MiB | 3451.7 MiB | 1133.2 MiB |

该历史快照中，1 GiB 仅容纳 334,852 个静态热点，动态容量为 0；2 GiB 起可容纳 362,132 个静态热点与 637,868 个动态槽位，容量覆盖全库后不再随预算增长。04 旧实验的静态热点为 355,354 个，不能混用。03/05 的 beam、预算验收和二进制口径与 04 不同，正式性能需按 run-id 与二进制哈希另行核对。[落地分析](docs/analysis/ours_formal_hot_dynamic_20260921.md) · [官方化交接](DiskANN官方化交接.md)

| 实现入口 | 作用 |
| --- | --- |
| [pca.rs](experiments/02_disk_shared_graph/native/src/pca.rs) | 路由规划、PCA 编码与查询投影；策略 `resident_full_else_pca1bit_m32_no_residual_v1` |
| [ours_records.py](src/disk_bench/ours_records.py) | validation 热点冻结与 test 参数/哈希核对；策略 `validation_hot_then_dynamic_fifo16_v1` |
| [record_cache.rs](experiments/02_disk_shared_graph/native/src/record_cache.rs) | 静态记录加载与缓存集成 |
| [dynamic_records.rs](experiments/02_disk_shared_graph/native/src/dynamic_records.rs) | 16 分片 FIFO、分部有效位与容量计账 |

每次正式运行保存 `.route_plan.json` 和热点清单哈希；test 沿用 validation 锁定的配置。


## 固定表示的缓存分配实验（2026-09-23 新增）

入口为 `scripts/run_ours_cache_allocation.py --tag <新标签> --binary <独立新二进制>`。
先测 GIST 0.5/4 GiB，32 workers、beam4、test800、九个宽度。两轮顺序为
`graph_first → records_only` 与 `records_only → graph_first`，参数、排名和表示冻结。
历史 05 结果不覆盖。独立正确性进程逐查询比较扩展节点序列、返回 ID、Recall 和访问计数；
详细路径日志不进入性能测量。全部新点继续要求独立参考及实测 RSS 准入。

I/O 按图读取、full4 验证触发的记录读取、rerank 触发的记录读取分类。
当前 compact/residual 打包存储，一次页读取可能同时包含两者，因此这些阶段计数不是
两个独立数据文件的物理读取量。三类请求/字节与总物理 I/O 逐查询核对，逻辑候选数量单列。
结果同时报告图/记录缓存占用、命中、I/O、Recall、QPS 和尾延迟；只在搜索路径一致时
讨论缓存分配效应。缓存对照完成后再做 06 的统一 4 GiB 常驻维度对照。

离线 PCA/索引准备与性能测量监督进程也隔离：干净监督进程启动 native，避免大型准备进程的 fork/exec 继承内存进入被测子进程峰值。native 的加载、预热、查询和退出仍采用原有 wait4 与采样 RSS/HWM 最大值验收，不扣除或重置峰值。

2026-09-23 用户调整：当前缓存对照只运行一轮，每档预算的 graph_first / records_only 各测一次，共四个性能配置。取消反序第二轮，仍复用已有图并保留独立参考和搜索路径校验；不据单轮结果宣称重复测量稳定性。

2026-09-23 宽度调整：已完成的九宽度结果保留，新对照只测 60/100/180，使用正式 `--search-widths` 参数及冻结锁。新队列为 `results/diagnostics/gist_cache_three_widths_20260923`，0.5 GiB 两策略全部通过准入和路径对照后，先导出 `figures/recall_qps_b0.5.svg`（同时提供 PDF/PNG/CSV 与来源清单），再继续 4 GiB。单轮不提供重复测量误差条。

2026-09-23 用户最终精简：取消扩展路径核对及其额外搜索。已完成 graph_first 保留，records_only 复用独立参考来源，直接测量后仅核对结果文件中的有序 ID、Recall 与计数；不宣称扩展路径已验证一致。绘图不再等待路径文件。旧诊断取消记录保留，新性能测量使用 results_only 后缀 run-id。

## 2026-09-23：validation 选择缓存策略（冻结后才测 test）

新增入口 `scripts/run_cache_selection.py --output <新目录>`，默认核查 GIST 的 0.5/4 GiB，复用 `gist_cache_three_widths_20260923` 的图、量化资产与 validation 热点排名。原 `--ours-cache-allocation` 及 graph_first 默认值不变。

每个预算先冻结表示，再对 graph_first / records_only 做 validation200、width=60/100/180，两轮反序串行测量。每宽度按查询总数/查询总秒数合并，两策略三个宽度的吞吐比取等权几何平均；records_only 必须两轮综合比均大于 1，且合并比至少 1.05 才胜出，否则选 graph_first。5% 是工程门槛，不代表统计显著。一方资源不合格则只可选择另一方；两方不合格或返回结果不一致，不输出有效选择。

`cache_selection.lock.json` 绑定二进制、表示、图与量化/热点资产、输入查询、CPU/NUMA、预算、三个宽度、validation 测量和规则。test 读取该文件，不重新选策略；变更绑定输入则拒绝。正确性参考独立进程；热点 profile 本身不冒充独立参考；每预算新增一次 validation 内存对照，四次测量复用。test 复用既有合格 test 证据，不记录展开路径。

缓存分配核查写入 `implementation_audit.json`。结果及准入证据写入新目录，不修改 native 原始 formal_ready；选择入口的 acceptance.json 单独证明本轮参考一致、预算、并发和锁的检查。已观察过的 test800 不称为未见测试集。已知 ANN/fio 进程监测不等于整机独占。

06 等本次两个预算选择通过后启动，4 GiB 所有表示使用同一个冻结策略，width 固定 60/100/180。
