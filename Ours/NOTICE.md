# Notice

The Python code in `Ours/` is original. The C++ algorithm core is vendored
under `Ours/core/hnswlib/` from `hnsw_rabitq` (Apache-2.0, locked commit
`c8ea9195d629e6f7140136bdb03d7b9ec5afc0df`). The repository also uses the
DiskANN3 framework in `baselines/diskann/` (Microsoft DiskANN, MIT License,
locked commit `3218478b5f7d1721840c29309e22d9fd74b1b4bc`).

See the respective `LICENSE` files inside each copied tree.
