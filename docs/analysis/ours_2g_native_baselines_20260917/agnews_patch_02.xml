<p>已落实新规则：Ours 主组默认 2 GiB/32 workers；baseline 不继承 Ours 限额，独立记录原生内存参数和真实峰值 RSS。调参与测试保持各方法自身配置不变，允许不同方法配置不同；聚合和绘图不再要求共同预算，图注明 Ours 2 GiB、baseline 原生配置。每个配置只跑一次，旧结果不自动追认。C++ 和 Rust 入口的 Recall 批次计时修复继续保留。</p>
