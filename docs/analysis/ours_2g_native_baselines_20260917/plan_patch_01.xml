<p>2026-09-17 最新规则：Ours=2 GiB，baseline 各自配置，每个正式配置只跑一次。此前统一 4 GiB 方案已被本规则取代。Ours 需以 cgroup 验证真实限额；baseline 默认只记录系统实测峰值 RSS，不新增统一硬限额。缺少 cgroup 委派只阻塞需要限额的运行，不作为 baseline 内存不同的失效理由。旧 RLIMIT_AS、对象求和和来源不明结果仍不能改标签充当新测量。第 10—11 节中的审计记录按原时点理解，不覆盖本规则。</p>
