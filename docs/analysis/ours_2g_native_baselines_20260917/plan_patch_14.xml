<pre caption="&#10;" lang="text"><code>主实验 workers = 32；预算扫描 workers = 32<br />formal repeats = 1，repeat_id = 0<br />input/metric/query split verified = true<br />native source, index and config fingerprints verified = true<br />config frozen before test = true<br />memory_enforcement = cgroup_v2 (Ours disk) / none (native baseline observation)<br />Ours: memory.max = 2147483648 bytes; memory.swap.max = 0; native baseline: no added cap<br />resource scope includes search startup, loading, scratch, warmup and queries<br />process peak RSS recorded for every method; cgroup peaks/events required for capped Ours; no failure<br />search wall-clock scope, query count and warmup verified = true<br />I/O and cache conditions audited = true<br />required metrics available; missing optional metrics explicitly null<br />formal_ready = true only after all applicable checks pass<br />05A resident: unconstrained_reference, io_backend=resident, direct_io=false</code></pre>
