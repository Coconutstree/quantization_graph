<p>资源 sidecar、真实进程峰值、单次实测结果及原始行已接入外层执行器；原生 CLI 到完整正式 artifact 的接入与各端口验收仍分别进行。分类开销用于解释总量，RSS 与 cgroup peak 是不同观测值，不能相加或互相替代；未测得字段为 null。预算失败行保留运行状态和证据，不填写伪造的 QPS=0 或 recall=0。</p><h3>7.1 四个主方法分别比较什么</h3><table><thead><tr><th><p>方法</p></th><th><p>保留的原生流程</p></th><th><p>内存规则与实验作用</p></th></tr></thead><tbody><tr><td><p>Ours-Disk</p></td><td><p>DB1 筛选、ExRaBitQ4、locality 布局、残差重排；冻结当前实现。</p></td><td><p>整个搜索进程执行 2 GiB cgroup 限额；作为被比较方法。</p></td></tr><tr><td><p>DiskANN-PQ-Disk</p></td><td><p>保留原生图搜索、PQ 候选评分、SSD 节点读取与精确评分；披露现有 reader 差异。</p></td><td><p>全量 PQ 短码及配置启用的缓存驻留；使用自身配置，报告真实峰值 RSS。</p></td></tr><tr><td><p>Starling-Disk</p></td><td><p>原生磁盘建图 → 抽样导航图 → 图分区与重布局 → 导航图引导的 page search；保留全量 PQ 和内存导航图。</p></td><td><p>使用自身配置；比较导航图与磁盘局部性优化后的完整系统，不套用 Ours 的 2 GiB 限额。</p></td></tr><tr><td><p>AiSAQ-Disk</p></td><td><p>建图和查询均启用 --use_aisaq；从 SSD 取 PQ 码，保留官方 inline/rearrange 与可选 PQ 缓存。</p></td><td><p>使用自身配置；比较 PQ 码移到 SSD 后的性能、内存与磁盘代价，DRAM-free 不等于进程 RSS 为零。</p></td></tr></tbody></table><p>Starling 与 AiSAQ 加入 05C 主系统对比；05A 继续比较量化器，05B 继续比较既定机制，不能把这两个完整系统改造成共享 Ours 图或量化器的端口。主图预登记四方法，实际曲线只使用通过该数据集验收的方法，缺失项保留状态。</p><p>论文依据：<a href="https://arxiv.org/html/2401.02116v3">Starling，SIGMOD 2024</a>；<a href="https://arxiv.org/html/2404.06004v2">AiSAQ: All-in-Storage ANNS with Product Quantization for DRAM-free Information Retrieval</a>。本节具体 CLI 参数以本地固定 release 为准，不把 release 的全部选项都当作原论文实验参数。</p><h3>7.2 Starling 与 AiSAQ 的配置登记</h3><p>下表是现有接入脚本的起点和必须登记的参数，不是已调优的正式配置，也不是要求两个 baseline 数值相等。最终值只在独立 validation 集上选择，连同实际索引头、完整命令和哈希冻结；test 不再修改。</p><table><thead><tr><th><p>项目</p></th><th><p>Starling-Disk</p></th><th><p>AiSAQ-Disk</p></th></tr></thead><tbody><tr><td><p>固定版本</p></td><td><p>17dc3e8a011533a62374445f53963e951b72883a；本轮源码检查通过，无登记补丁。</p></td><td><p>f0a48e984c685bd498e3c4f88386b47e0e4ab1ac；本轮源码检查通过，现用构建含已登记 sysfs 扇区查询兼容补丁。</p></td></tr><tr><td><p>图与 PQ</p></td><td><p>GIST 已验证 R48，L_build=400；目标 PQ=32 B，现存 GIST 索引实际为 31 B，以索引头为准。其他数据先查页容量。</p></td><td><p>脚本起点 R64、L_build=400、PQ=32 B；须用目标原维度样本验证布局，再选择实际图度数与码长。</p></td></tr><tr><td><p>方法特有建索引参数</p></td><td><p>脚本起点：导航抽样比例 0.01、导航 R48/L128、分区迭代 8；执行完整 partitioner 和 index_relayout。</p></td><td><p>--use_aisaq；脚本默认 inline_pq=-1（按页余量自动选择）、rearrange 关闭。inline 数量和重排作为原生配置登记。</p></td></tr><tr><td><p>方法特有搜索参数</p></td><td><p>--use_page_search=1、--mem_L 非零；脚本起点 mem_L=32、use_ratio=1、use_sq=0。</p></td><td><p>--use_aisaq、pq_read_io_engine=aio；vector_beam 默认 1，且不得大于 beam。</p></td></tr><tr><td><p>缓存</p></td><td><p>登记节点缓存和导航图；不能为统一缓存口径删除导航图或全量 PQ。</p></td><td><p>登记节点缓存、共享 PQ 缓存和每线程 PQ 读页缓存。读页缓存要求 rearrange；32 线程容量须合计并包含元数据开销。</p></td></tr><tr><td><p>搜索工作负载</p></td><td><p>k=10、workers=32、每配置一次；L、beam 按本方法 validation 冻结。GIST 旧诊断 beam=1。</p></td><td><p>k=10、workers=32、每配置一次；L、beam 按本方法 validation 冻结。脚本 beam 起点为 4。</p></td></tr><tr><td><p>外层内存</p></td><td><p>默认不新增统一硬限额，报告实际峰值 RSS。旧 AS=4 GiB 诊断保留原标签。</p></td><td><p>默认不新增统一硬限额，报告实际峰值 RSS；PQ 缓存容量不等于整个进程内存。</p></td></tr></tbody></table><p>两个官方 CLI 的起始搜索宽度为 20/40/80/160/320，仅用于制定 validation 扫描；候选范围是否覆盖 0.90/0.95/0.99 目标召回率须先核验，不能把相同 L 当作相同搜索工作量。脚本默认 node_cache=0、AiSAQ 的两个 PQ 缓存为 0，只是起点，正式配置允许按各自方法调优。</p><p>AiSAQ 的 sysfs 补丁仅替代设备扇区大小查询，不修改图、PQ、候选队列或搜索逻辑；仍需在报告中披露。已存无补丁验证完成建索引，但查询失败，环境记录指向设备节点访问问题，不能把兼容补丁版称为完全无修改原版。</p><h3>7.3 三个 core 数据集的支持与验收状态</h3><table><thead><tr><th><p>数据集</p></th><th><p>Starling-Disk</p></th><th><p>AiSAQ-Disk</p></th></tr></thead><tbody><tr><td><p>GIST，D=960</p></td><td><p>R48 的完整原生流程小样本通过，百万索引已存在；w32/as4g 诊断 result.json 为 done，但 formal_ready=false、throughput_comparable=false。</p></td><td><p>已有 1024×64、16 query 通用功能验证；当前证据尚不足以证明 GIST 目标维度和全量正式验收通过。</p></td></tr><tr><td><p>AGNews，D=1024</p></td><td><p>当前固定版本 FP32/R64 路径单节点 4356 B，超过 4096 B；已存状态 blocked。仅降低图度数不能解决 FP32 向量占满一页的问题。</p></td><td><p>列为主 baseline 的计划验收项；目标维度、完整索引、正确性和计时待验证，不根据论文或方法名称直接标 passed。</p></td></tr><tr><td><p>DBpedia，D=1536</p></td><td><p>当前固定版本 FP32/R64 路径单节点 6404 B，超过 4096 B；已存状态 blocked。其他原生配置需独立验证后才能改变支持状态。</p></td><td><p>列为主 baseline 的计划验收项；目标维度、完整索引、正确性和计时待验证。</p></td></tr></tbody></table><p>GIST 的 FP32/R64 节点长 4100 B，也超过一页；因此不能直接把通用脚本默认 R64 套到 Starling 的 GIST 正式命令。AGNews/DBpedia 不通过加内存、截断维度或自行改页长补齐；若验证出完整可用的官方压缩路径，则另立配置、披露输入与存储差异。这里的 unsupported 仅针对所查版本和路径，不声称 Starling 算法对所有实现都不支持。</p><h3>7.4 已加入方法集合，还差哪些执行工作</h3><p>本轮重新核对 native_contract.py 的 METHOD_SPECS/LAYER_METHODS 与 ports.local.json：四方法主集合已包含 Ours、DiskANN、Starling、AiSAQ。Starling/AiSAQ 的注册状态仍为 pending；官方 CLI 可生成命令计划并记录原生结果，但尚不能作为完整 05-suite 正式端口运行。加入文档和方法列表不等于完成正式接入。</p><p>后续按方法完成：核验目标维度与输入/结果 ID → 连接正式 artifact 与外部 RSS 测量 → 验证纯搜索计时和 CPU/NUMA/预热条件 → validation 调参 → 冻结源码、二进制、索引和配置 → 每个正式配置测一次 → 在共同达到的 Recall 区间绘图。缺少可选逐查询指标时写 null；必需的 Recall、QPS、RSS 与溯源证据必须真实可核验。</p><p>run_official_disk_baseline.py 默认只输出命令计划；--execute 会执行建索引和搜索。默认不传 --search-memory-gib，也不要求 cgroup。只有单独研究该 baseline 的限额时才显式传 --search-memory-gib 和 --cgroup-parent。建索引 -M/-B 不能当作整个搜索进程的硬限额。现有 CLI 仍为诊断接口，本轮不执行全量实验，不自动把 pending 改成 ready。</p><p>本轮证据：docs/analysis/plan_document_audit_20260917/source_registry_check.json；docs/analysis/official_disk_baselines_20260916/smoke_validation.json；GIST Starling 的 diagnostics/w32_as4g_20260917/result.json 及 AGNews/DBpedia 的 Starling-Disk/result.json。Ours 与 DiskANN 实际二进制哈希仍不同于登记值，需独立核验后更新登记，不能因新增两个 baseline 自动放行。</p>
