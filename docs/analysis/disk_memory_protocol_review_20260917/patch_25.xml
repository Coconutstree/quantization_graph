<h2>11. 2026-09-17 核验结论与待修复项</h2><table><thead><tr><th><p>问题</p></th><th><p>本次证据</p></th><th><p>处理 / 状态</p></th></tr></thead><tbody><tr><td><p>内存口径混用</p></td><td><p>原文称 search-index DRAM，现有 Starling runner 使用 resource.RLIMIT_AS；native_contract 对几类自报字节求和。</p></td><td><p>方案改为总 cgroup 预算 + 实测峰值 + 分类解释；执行器待接入。</p></td></tr><tr><td><p>入口硬编码 2 GiB</p></td><td><p>orchestrator.py 拒绝 search_dram_budget_gib!=2.0；native_contract 的完整性检查含 2.0 与旧矩阵。</p></td><td><p>按显式 protocol/matrix 配置验收；不能只把参数改成 4 就宣称完成。</p></td></tr><tr><td><p>32 线程的实际开销</p></td><td><p>GIST Starling 每线程坐标缓冲 60 MiB，32 线程合计 1920 MiB；已存诊断峰值 RSS 约 2.01 GiB。</p></td><td><p>4 GiB 是预检起点；2 GiB 失败有实现资源原因；从 AS 改成 cgroup 也不保证 2 GiB 能跑。</p></td></tr><tr><td><p>Starling 布局限制</p></td><td><p>R64 在三个目标维度小样本构建失败；GIST R48 完成索引；AGNews/DBpedia 当前 FP32 跨页路径阻塞。</p></td><td><p>独立标记 unsupported_layout；不改页长、维度或算法补齐。</p></td></tr><tr><td><p>05C 方法过期</p></td><td><p>revision 52 仍含旧四 baseline；本地已确认原生 DiskANN、AiSAQ、Starling。</p></td><td><p>更新本篇主方法集合；移植版单列补充。</p></td></tr><tr><td><p>消融并非全部单因素</p></td><td><p>ours_port.rs 的 locality 要求 coalescing+reuse；resident/no-gate 还需核对 query codec。</p></td><td><p>固定支持各开关的布局做消融；完整 locality 系统另测。</p></td></tr><tr><td><p>数据大小混用</p></td><td><p>头部 N/D 与主要表项一致；旧大小列混用了目录和输入文件。</p></td><td><p>新版统一列 base 文件 GiB，保存头部核验；全量输入哈希运行前再确认。</p></td></tr><tr><td><p>旧通过标记不能复用</p></td><td><p>官方 CLI 接入与新资源/计时契约不是同一件事。</p></td><td><p>来源、支持性、资源和计时均通过后才设置 formal_ready。</p></td></tr></tbody></table><h2>12. 内存预算如何执行与比较</h2><p>12.1 三种量分开：常驻索引字节解释算法规模；进程峰值 RSS 解释实际驻留；cgroup memory.peak 解释资源组总占用。VmPeak/虚拟地址空间只作诊断。GiB 固定为 2^30 B，论文中的 GB/MB 保留原单位，不擅自视为完全相同。</p><p>12.2 每次独立 repeat 建立新 cgroup，原生搜索进程在 exec 和任何索引加载之前加入；子进程也在组内。设置 memory.max 为预算字节数、memory.swap.max=0，记录实际层级和父组约束；避免继承旧 2 GiB RLIMIT_AS 形成第二个更紧的限制。观察器放在组外。缺少 memory controller、委派权限或可靠峰值采集时记录 blocked_environment，不静默降级为 RLIMIT_AS 或仅 RSS 采样。</p><p>12.3 总预算包括索引、码本、所有 worker 的队列/visited/LUT/I/O 缓冲、方法缓存、查询输入缓冲、运行时，以及 cgroup 计入的文件缓存和内核内存。GT 评估放到搜索进程外；原生 CLI 若必须加载 GT，则如实计入，不能事后减掉。建索引单独测内存与时间；搜索加载计入内存峰值，QPS 计时从纯查询区间开始。峰值缺少阶段划分时准确标为进程生命周期峰值，不能假称只测查询。</p><p>12.4 memory.peak、memory.current、memory.stat、memory.events 与 swap 记录在每次运行保存；峰值 RSS 同时从原生进程/可靠系统接口获取。若只能采样，字段明确写 sampled，不能冒充无遗漏峰值。原生 CLI 多个搜索宽度复用一个进程时，只能共享该进程生命周期峰值；主实验优先每个 operating point 单独进程，便于对齐内存和性能。</p><p>12.5 固定目标 SSD、CPU/NUMA 与方法运行顺序策略；同一时刻只有一个被测系统，构建、fio 和其他基准不与搜索争用目标盘。预热 query 列表和次数在 validation 上确定并冻结，所有方法同协议；方法内部缓存容量/生命周期允许不同但全部计入预算。O_DIRECT 不能消除设备缓存。涉及 buffered/mmap 时还需核验页缓存归属与预热，否则单列结果；不能假定新建 cgroup 自动清空已存在的系统缓存。</p><table><thead><tr><th><p>实验组</p></th><th><p>固定条件</p></th><th><p>变化量</p></th><th><p>允许的结果</p></th></tr></thead><tbody><tr><td><p>05C 主性能</p></td><td><p>32 线程；拟 4 GiB；输入、设备、预热和统计协议一致</p></td><td><p>各方法冻结的官方配置与搜索强度</p></td><td><p>共同 recall 下的性能和实际用量；支持缺失明确列出。</p></td></tr><tr><td><p>05C 预算扫描</p></td><td><p>32 线程；同一任务与硬件</p></td><td><p>1/2/4/8 GiB；各预算仅通过官方参数调优</p></td><td><p>可运行/预算不足/未达到目标 recall，及实际 QPS、内存、磁盘成本。</p></td></tr><tr><td><p>05A/05B 机制实验</p></td><td><p>保留原候选或图控制关系；统一新磁盘预算</p></td><td><p>编码或已披露的机制状态</p></td><td><p>解释组件贡献，不能冒充全部同图的完整系统比较。</p></td></tr><tr><td><p>线程 scaling（补充）</p></td><td><p>固定一个预算与同一数据任务</p></td><td><p>1/4/8/16/32 worker，所有方法同组</p></td><td><p>单独画并发曲线，不与 w32 预算曲线混合。</p></td></tr></tbody></table><p>4 GiB 预检只确认合法输入、正确性、资源与运行可行性，不用测试集上的赢家来选择预算。如果某原生方法仍不适配，记录原因并继续其他方法；8 GiB 保留为已登记扫描点，不为单个方法偷偷放宽主预算。预算内不要求用满内存，不强制相同 PQ 字节、导航比例或额外缓存。</p><p>当前 core 数据的原始向量约为几 GiB，4/8 GiB 预算可能容纳其中一些完整 base。仍需验证被测路径确实按声明从 SSD 读取索引；此时结果只能支持当前数据规模上的磁盘系统表现，不能据此声称数据量必然大于内存或证明十亿规模扩展性。MSMARCO 的 DB1 下界超过本轮最高预算，报告 Ours 的限制，不伪造完整扫描曲线。</p><h2>13. 执行顺序、输出和验收</h2><table><thead><tr><th><p>步骤</p></th><th><p>工作</p></th><th><p>完成标准</p></th></tr></thead><tbody><tr><td><p>1 冻结来源</p></td><td><p>固定 baseline commit、子模块、编译参数、二进制哈希及实际差异；Ours 保存源码快照。</p></td><td><p>官方原生/兼容诊断版本明确区分。</p></td></tr><tr><td><p>2 核验输入</p></td><td><p>base/query/GT 一致；验证向量值、ID、度量和划分；完整哈希写入 run manifest。</p></td><td><p>每方法读到同一任务；已有测试若用于调参需标探索性或另立未使用测试集。</p></td></tr><tr><td><p>3 复用或导出索引</p></td><td><p>先核验已有图、码本、布局与参数哈希；变化才重建对应产物。</p></td><td><p>完整原生路径正确、支持当前维度；无需全部重新建图。</p></td></tr><tr><td><p>4 接入资源控制</p></td><td><p>外层实现 cgroup launcher、字节预算、环境预检、峰值/events 采集及状态分类。</p></td><td><p>以独立小程序验证组内执行和限制生效；不修改被测搜索算法。</p></td></tr><tr><td><p>5 更新契约</p></td><td><p>替换硬编码 2 GiB、旧五方法、隐含缓存/计数要求；新增 protocol_id 与预算矩阵。</p></td><td><p>缺失可选指标可如实表示，必需资源与计时证据不放宽。</p></td></tr><tr><td><p>6 validation 调参</p></td><td><p>每方法、每预算登记合理候选范围和计算/建索引投入；达到目标 recall 的配置在测试前冻结。</p></td><td><p>保留所有尝试和配置，不能逐条测试结果择优回填调参。</p></td></tr><tr><td><p>7 正式运行</p></td><td><p>每点独立 5 次，方法次序交错/随机；相同预热，隔离后台干扰。</p></td><td><p>纯搜索批次计时已核验；输入数量和顺序相同；失败记录保留。</p></td></tr><tr><td><p>8 聚合与绘图</p></td><td><p>按 dataset/protocol/threads/budget/method/config 分组。</p></td><td><p>中位数与 IQR 溯源到 5 次；各预算不可混聚合；缺失和失败有矩阵。</p></td></tr></tbody></table><p>QPS 定义为计时区间内实际完成查询数除以查询批次墙钟时间，排除建索引、加载、预热、GT 评估与结果文件写出。不能把 /usr/bin/time 的整个程序耗时直接当作纯搜索时间；原生计时范围未核验则 throughput_comparable=false。若短查询批次需要重复 epoch，应冻结 epoch 数和缓存策略，在每方法相同条件下测量，并单独注明重复查询负载。</p><p>主要图表：① 各数据集在冻结主预算下的 Recall@10–QPS 曲线；② 目标 recall 下的预算–QPS 曲线与方法可运行矩阵；③ 实际峰值内存与磁盘索引大小；④ 05A/05B 组件证据图。目标 recall 表报告实测 recall 与预登记的容差/选点规则，无法覆盖目标则留空，不外推加速比。p95 仅在原生数据可得时报告；p99/P99.9 和内部计数不能从 batch 均值推造。</p><table><thead><tr><th><p>状态</p></th><th><p>判定依据</p></th><th><p>报告方式</p></th></tr></thead><tbody><tr><td><p>passed</p></td><td><p>来源、输入、正确性、资源、计时与重复验收全部通过</p></td><td><p>可进入对应正式图表。</p></td></tr><tr><td><p>budget_exceeded</p></td><td><p>已证明不可避免的驻留下界超限，或实际分配失败/OOM 且有资源证据</p></td><td><p>报告具体预算/线程/配置；不据一次配置失败宣称该算法全部配置都不可能。</p></td></tr><tr><td><p>unsupported_layout / metric</p></td><td><p>固定原生版本不支持当前输入布局或度量</p></td><td><p>保留支持范围说明，不改算法补齐。</p></td></tr><tr><td><p>runtime_error</p></td><td><p>运行失败但尚无充分内存证据</p></td><td><p>保存日志与退出码，不能把所有 SIGSEGV 归为 OOM。</p></td></tr><tr><td><p>pending / blocked_environment</p></td><td><p>尚未完成或控制器/运行环境不足</p></td><td><p>不进入正式性能比较。</p></td></tr></tbody></table><p>此次修订只更新实验文档和核验记录，没有实现 cgroup runner、修改算法、重新建图、启动搜索或升级任何已有结果的 formal 状态。后续先完成步骤 4—5，再运行新预算测量；现有 AS=4 GiB 的 Starling 诊断不能直接作为 cgroup=4 GiB 的正式结果。</p><h2>14. 三篇论文与内存限制的依据</h2><p>DiskANN 原论文 §3—4 以 64 GB RAM 单机为容量与性能背景，内存保存 PQ 码和缓存，SSD 保存图与原向量；这不是 RLIMIT_AS=64 GB 的证据。Starling §6.1、附录 N 默认每 segment 2 GB 内存/10 GB 磁盘、8 查询线程，调导航图等配置满足预算；原文不足以确认它完整计入线程工作区或用哪种 OS 限额。AiSAQ §4.2 用 /usr/bin/time 测查询峰值内存，仅加载 10 条 query、不加载 GT；约 11—14 MB 不能直接与 32 线程、整批查询配置比较。</p><p><a href="https://harsha-simhadri.org/pubs/DiskANN19.pdf">DiskANN 原论文</a></p><p><a href="https://arxiv.org/html/2401.02116v3">Starling 原论文 §6.1 与附录 N</a></p><p><a href="https://arxiv.org/html/2404.06004v2">AiSAQ 原论文 §4.2—4.3</a></p><p>本方案借鉴固定资源条件和实际内存测量，但 32 线程、4 GiB 及预算扫描属于本项目协议。Linux RLIMIT_AS 限制虚拟地址空间；cgroup v2 memory.max 约束资源组所计入内存，memory.swap.max=0 禁止该组使用 swap。cgroup 用量可包含文件缓存、内核内存，且内核允许某些场景短暂超过 memory.max，所以应同时保存统计与事件，避免将其简化成进程 RSS 的绝对上限。</p><p><a href="https://man7.org/linux/man-pages/man2/getrlimit.2.html">Linux getrlimit：RLIMIT_AS 定义</a></p><p><a href="https://docs.kernel.org/admin-guide/cgroup-v2.html">Linux cgroup v2 内存控制文档</a></p><p>本地实现证据：experiments/05_disk_system_fair/orchestrator.py、native_contract.py、run_starling_existing_layout.py、native_rust/src/ours_port.rs；诊断证据 docs/analysis/starling_memory_recovery_20260917/README.md；输入头部核验保存在 docs/analysis/disk_memory_protocol_review_20260917/input_header_check.json。</p><h2>15. AGNews 历史实验核验补充</h2><p>2026-09-17 通过飞书 CLI 读取 AGNews 分析 revision 316，并核对其 CSV、artifact 与 preflight。四种旧方法各 40 个 w32 宽度点，全部只有 repeat=0；Ours cache_bytes=26259456，其他三个为 0。OG-LVQ/Symphony 的 peak_rss_bytes 分别为 12304/12288，恰为 resident+scratch 求和，不能作为真实进程峰值；因此原分析“还剩约 1 GiB 可做缓存”缺少实际内存依据。</p><p>两个 run 的 preflight 均标记 hdd_raid/rotational=true，不能作为已验证 NVMe SSD 条件。各方法最高 QPS 对应不同 recall；文中平均 latency 是跨 40 个宽度点的平均，不能用于同 recall 排名。Symphony 使用已撤回的 neighborfetch 旧端口，parity 标签不足以证明原生等价。原文已有撤回提示，但后续“为什么快”和优化收益排序仍需一并限制为历史假设。</p><p>这些问题通过新版总预算、原生实现准入、固定设备与预热、同 recall 比较及 5 次独立重复分别处理，不能仅靠把 2 GiB 改成 4 GiB 解决。AGNews 当前 Starling 页容量支持问题仍单独记录。详细本地核验：docs/analysis/disk_memory_protocol_review_20260917/AGNEWS_CONFIG_AUDIT.md。本次不改写 AGNews 分析原文。</p><p><a href="https://my.feishu.cn/docx/AhoKdQxEBogmWoxfk6xcxHKjnsg">AGNews 磁盘环境 32 线程实验分析（本次读取核验对象）</a></p>