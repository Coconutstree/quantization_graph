<h2>二、步骤 1：先把所有向量减去同一个均值</h2>
<p>先计算整个数据库的平均向量，再让每条数据都减去它。比如平均向量是 [10, 100]，一条数据是 [11, 102]，减完就得到 [1, 2]。查询向量也用同一个均值处理。</p>
<p>这样做相当于一起移动坐标原点。两点都减去同一个数，它们的差不变，所以这一步不会改变真实距离。它的作用是减少各维本身的固定偏移，方便后面压缩。</p>
<h2>三、步骤 2：每个向量用自己的刻度，把小数变成编号</h2>
<p>以下用 <b>LVQ4</b> 举例：每个坐标用 4 bit，能保存 0～15 共 16 个编号。对一条已经减过均值的向量，先找出它自己最小、最大的坐标，再在这个范围内放 16 个等距刻度。</p>
<p>例如向量是 [−1, 0.34, 1.12, 2]。最小值是 −1，最大值是 2，所以刻度间隔为 (2 − (−1)) ÷ 15 = 0.2。每个坐标保存最近刻度的编号：</p>
<table><thead><tr><th><p>原来的坐标值</p></th><th><p>保存的编号</p></th><th><p>根据编号恢复的近似值</p></th></tr></thead><tbody><tr><td><p>−1</p></td><td><p>0</p></td><td><p>−1 + 0 × 0.2 = −1</p></td></tr><tr><td><p>0.34</p></td><td><p>7</p></td><td><p>−1 + 7 × 0.2 = 0.4</p></td></tr><tr><td><p>1.12</p></td><td><p>11</p></td><td><p>−1 + 11 × 0.2 = 1.2</p></td></tr><tr><td><p>2</p></td><td><p>15</p></td><td><p>−1 + 15 × 0.2 = 2</p></td></tr></tbody></table>
<p>因此，这条向量保存的是 [0, 7, 11, 15]，再加上恢复数值所需的起点和间隔。四个坐标的编号只占 2 字节；范围参数也要占空间，不能只算编号。实际高维向量更容易摊薄这部分额外开销。</p>
<p><b>“局部自适应”指每个向量都有自己的刻度。</b>另一条向量如果只落在 [−0.1, 0.2]，它的间隔就是 0.02。同样编号 7，在两条向量中表示的数值可以不同。因此 LVQ 不能把不同向量的编号直接相减来算距离。</p>
<h2>四、步骤 3：可选地再存一点误差，让最后排序更准</h2>
<p>刚才 0.34 被恢复成 0.4，二者差值是 −0.06。这个“原值 − 恢复值”就叫残差。论文可以用第二层短码再压缩这点残差。</p>
<p>搜索大部分节点时，只使用第一层码；最后只对留下来的候选读取第二层码，把恢复值修正得更接近原值，再排一次序。例如把第一层恢复值 0.4，加上第二层给出的近似残差，就会更接近 0.34。</p>
<p>名称 <b>LVQ-4×8</b> 表示每坐标第一层 4 bit、第二层 8 bit。第二层需要额外存储，也需要额外计算。论文包含不同位宽的单层和双层配置；我们的磁盘实验只用了单层 LVQ4。</p>
<h2>五、步骤 4：用压缩向量建立图</h2>
<p>图可以理解为“每条数据的一份邻居名单”。论文采用 Vamana 式建图：为一个点寻找一批邻居候选，删掉部分作用重复的连接，并限制每个点保留的边数。对全库重复这个过程，得到可以搜索的图。</p>
<p>这些建图步骤也需要大量距离计算。论文分析了量化误差对构图的影响，并通过实验研究使用 LVQ 压缩向量构图的可行性，从而减少建图期间向量占用的内存。这里的图仍然负责导航，LVQ 负责提供计算距离的紧凑表示。</p>
<h2>六、步骤 5：查询时保存什么、怎样算一个距离</h2>
<p>原论文针对内存检索。主要保存：邻居名单、入口、每条向量的第一层码及其刻度、全库均值；双层版本还保存残差码。查询时另外开一份候选池。</p>
<p><b>查询向量保持浮点数；数据库向量按需要从短码恢复数值。</b>距离计算可以一边恢复一边累加，不用提前解压整个数据库。</p>
<p>沿用前面的例子，假设减过均值的查询向量是 [−1, 0.5, 1, 2]，数据库向量恢复后是 [−1, 0.4, 1.2, 2]。逐坐标相减、平方后相加：0² + 0.1² + (−0.2)² + 0² = 0.05。这就是用来比较远近的近似分数，越小越近。</p>
<h2>七、步骤 6：顺着图搜索，最后返回结果</h2>
<ol><li>先给入口算一个距离，把它放入候选池。</li><li>从候选池中挑距离较近、还没查看过邻居的点。查看它的邻居名单，这个动作叫“展开节点”。</li><li>给这些邻居算距离，更新候选池；池子满了，就淘汰距离较大的候选。</li><li>重复上两步，直到候选池里没有需要继续展开的点。</li><li>单层版本按现有分数返回；双层版本先用残差码重新评分，再返回前 k 个。k 就是用户要求的结果个数。</li></ol>
<p><b>候选池容量决定同时保留多少条搜索线索，不等于总共只访问这么多个点。</b>例如容量是 40、最终返回 10 个，搜索中可以不断淘汰旧候选、加入新候选，所以累计评分数可以远大于 40。</p>
<h2>八、为什么能更快，又为什么会漏掉近邻</h2>
<p>压缩后，每次从内存搬来的数据更少；论文还利用一次处理多个坐标的 CPU 指令和提前读取数据来加速。查询向量没有被压缩，并不意味着距离就是精确的：数据库向量已经变成近似值，候选的先后顺序可能改变；有限候选池也可能过早丢掉有用的路径。</p>
<p>第二层残差能改善已找到候选之间的排序，不能补回搜索时从未找到的点。</p>
<p><a href="https://www.vldb.org/pvldb/vol16/p3433-aguerrebere.pdf">原论文：Similarity search in the blink of an eye with compressed indices，§3–5</a></p>
<h1>第二部分：我们的磁盘实验改了什么</h1>
<p>我们先用 SVS 构建并保存图与单层 LVQ4 编码，再把邻居名单和压缩向量分别放进两个磁盘文件。全库均值、索引说明和查询工作区留在内存。查询需要哪条记录，就读取它所在的 4 KiB 页；本次查询读过的页可以缓存复用。</p>
<p>下图只需先看三列：左边是内存，中央是磁盘，右边是读取顺序。graph.pages 保存邻居名单，lvq4.pages 保存压缩向量；“码行”就是一个向量的编号和恢复参数。</p>
