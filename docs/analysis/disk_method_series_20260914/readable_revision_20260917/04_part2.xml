<h2>一、搬到磁盘后，一次查询这样走</h2>
<ol><li>取入口的 LVQ4 记录，算距离，放入候选池。</li><li>选择要展开的点 u，读取它的邻居名单。</li><li>对需要评分的邻居 v，先取得 v 的 LVQ4 记录，再算距离、更新候选池。所在页没有缓存时，这一步需要读 SSD。</li><li>继续搜索，最后按 LVQ4 分数返回前 10 个；当前配置没有第二层残差重排，也没有原始浮点向量重排。</li></ol>
<p><b>主要变化发生在第 3 步：为了判断邻居值不值得继续搜索，就可能要先读盘。</b>例如展开 u 后发现了 64 个邻居，需要访问相应的编码记录；但读取次数不一定是 64，因为多条记录可以在同一页，已经缓存的页也不用再读。DiskANN 的全库 PQ 码在内存，这类邻居评分通常不需要为编码另读 SSD。</p>
<h2>二、这版实现能代表什么</h2>
<p>当前磁盘查询的距离计算仍是本地解码实现，没有完成与官方优化距离内核的一致性核验，因此 OG-LVQ 的正式实验准入仍被阻塞。它的历史曲线反映的是这版移植实现，不能据此断言原论文 OG-LVQ 本身比 DiskANN 慢。</p>
<p>原存储图中的 seen 是旧版标注：当前源码使用官方 SearchBuffer 候选队列，默认不启用独立的 visited 去重过滤。本文保留原图资源，以此处说明为准。</p>
<p>核验位置：experiments/05_disk_system_fair/native/og_lvq_disk_port.cpp 的 lvq_distance 与 search_one；docs/analysis/baseline_algorithm_audit_20260916/README.md。本文改写没有运行新实验。</p>
