<h2>三、实际查询顺序，以及增加的读盘开销</h2>
<ol><li>先把查询向量转成 SQ4U 编号。</li><li>取得入口的编码，计算整数距离，放入候选池。</li><li>选择要展开的点 u，读取它的邻居名单。</li><li>对还没有评分过的邻居 v，取得 v 的编码，计算整数距离并更新候选池。编码所在页没有缓存时，需要读 SSD。</li><li>重复搜索，最后按整数距离返回前 10 个；当前配置没有用原始浮点向量再排一次序。</li></ol>
<p><b>短码减少了索引大小，但不保证一次评分只从磁盘读取几个字节。</b>缓存未命中时仍然要读一整页；图页和编码页分开保存，也需要分别取得。因此，实际性能既取决于 NSG 找路的效率，也取决于候选编码分布在哪些页上。</p>
<h2>四、当前实现与历史曲线的区别</h2>
<p>当前源码复用 Glass 的整数距离计算和 LinearPool 候选队列。此前本地队列处理相同距离候选的顺序与官方不同，已经在源码中修正；小规模对照通过，但旧性能二进制和历史曲线没有因此自动更新。正式准入仍需要完成，不能把旧曲线当作修正后实现的结果。</p>
<p>核验位置：experiments/05_disk_system_fair/native/glass_disk_port.cpp 的 search_one；baselines/pyglass/glass/quant/sq4u_quant.hpp、calibrator.hpp；docs/analysis/baseline_algorithm_audit_20260916/README.md。本文改写没有运行新实验。</p>
