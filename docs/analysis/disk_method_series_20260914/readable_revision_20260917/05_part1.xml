<h2>二、步骤 1：先给每个点找一批附近的点</h2>
<p>先建立一张近似 kNN 图：对每个点，找 k 个大致较近的点作为邻居。kNN 的意思就是“k 个最近邻”。<span background-color="light-yellow" text-color="red"><b>这张初始图只提供候选连接，后面还要重新筛选，才得到最终的 NSG。</b></span></p>
<p>原因是：一个点最近的许多邻居可能集中在同一小片区域。把连接机会都用在这些点上，搜索仍然可能绕远路。NSG 希望留下数量较少、能通向不同位置的有用连接。</p>
<h2>三、步骤 2：选一个所有查询共用的入口</h2>
<p>计算数据库的平均向量，再在初始图中搜索一个靠近它的真实数据点，把这个点作为入口。平均向量用来帮助找入口，入口本身仍然是数据库里的一条数据。</p>
<h2>四、步骤 3：给每个点收集更有用的邻居候选</h2>
<p>假设现在要给点 u 选邻居。暂时把 u 当成查询目标，从入口沿初始图搜索它。搜索途中经过的点，加上 u 原来的近邻，组成待选名单。</p>
<p>这样待选名单里既有 u 附近的点，也有从入口接近 u 时经过的点。后者可能提供有用的导航连接。这个过程要对每个数据点执行一次。</p>
<h2>五、步骤 4：删掉作用重复的边，保留有用的方向</h2>
<p>按离 u 从近到远处理待选点，先保留最近的一个。如果已有邻居能更靠近另一个待选点，就不再给 u 加这条直接连接；否则保留它，直到达到边数预算或候选用完。</p>
<p>用一个二维例子看这条规则：u=(0,0)，待选点 A=(1,0)、B=(2,0)、C=(0,2)。先保留最近的 A，然后检查 B 和 C：</p>
<table><thead><tr><th><p>待选点</p></th><th><p>与已经保留的 A 比较</p></th><th><p>对 u 的选边结果</p></th></tr></thead><tbody><tr><td><p>A</p></td><td><p>离 u 最近，距离为 1。</p></td><td><p>保留 u→A。</p></td></tr><tr><td><p>B</p></td><td><p>A 到 B 的距离是 1，小于 u 到 B 的距离 2。</p></td><td><p>不保留 u→B；A 已提供一个更接近 B 的中继位置。</p></td></tr><tr><td><p>C</p></td><td><p>A 到 C 的距离约为 2.24，大于 u 到 C 的距离 2。</p></td><td><p>保留 u→C；仅靠 A 不能替代这条连接。</p></td></tr></tbody></table>
<p>最终 u 在这个例子中连向 A 和 C。这个规则倾向于避免把连接都花在相似方向上；它不会自动添加 A→B，整张图能否从入口走通还需要下一步检查。</p>
<h2>六、步骤 5：检查有没有从入口到不了的点</h2>
<p>选完边后，从入口顺着有向边遍历整张图。如果还有一片区域到不了，就添加连接，再继续检查，直到所有点都能从入口到达。这个步骤叫可达性修复。</p>
<p>“有路可达”表示连接关系允许走过去。实际查询只查看有限数量的候选，所以仍然可能没走到真正的近邻。</p>
<h2>七、步骤 6：图建好后，一次查询怎么走</h2>
<p>索引保存原始向量、每个点的邻居名单和入口。查询时维护一个有容量限制的候选池：它是一份“目前找到的较近点”的名单，距离小的排在前面，并记录哪些点已经查看过邻居。</p>
<ol><li>从入口开始，计算它到查询向量的距离。</li><li>挑一个较近且尚未展开的候选，查看它的邻居名单。</li><li>计算新邻居到查询的距离，加入候选池；容量不够时淘汰较远的点。</li><li>继续展开候选，直到池中没有需要继续处理的点，再返回最前面的 k 个结果。</li></ol>
<p>例如继续使用前面的 u、A、C，令查询 q=(2,2)。它们到 q 的平方距离分别是 8、5、4，所以发现 A、C 后会优先展开 C。假设 C 又连着 D=(1,2)，D 的分数是 1，搜索就找到一个更近的候选。距离分数按“坐标差的平方相加”计算，越小越近。</p>
<p>搜索并非永远只走一条路：候选池还保留 A 等备用候选。容量 L 越大，能同时留下的搜索线索越多，通常也需要更多计算。L 是搜索候选池容量，R 是建图时每个点的出边预算，k 是最终返回个数，三者不同。</p>
<h2>八、NSG 的收益和误差来自哪里</h2>
<p>NSG 希望用较少的边和较短的搜索路径找到近邻，从而减少距离计算。原论文这一流程使用原始向量评分，近似主要来自图连接和有限范围的搜索：没有被搜索到的点，就不会出现在结果中。</p>
<p><a href="https://arxiv.org/abs/1707.00143v9">原论文：Fast Approximate Nearest Neighbor Search with the Navigating Spreading-out Graph，Algorithm 1、2</a></p>
<h1>第二部分：我们的磁盘实验改了什么</h1>
<h2>一、先用 Glass 建 NSG，再选择 SQ4U 算距离</h2>
<p>实验配置名 NSG_R64_L400_SQ4U 表示使用 NSG 图、出边预算 64、建图搜索宽度 400，以及 SQ4U 编码。这里的 400 是建图参数，查询时还会单独设置搜索宽度。</p>
<p>SQ4U 用一套共享刻度，把数据库向量的每个坐标变成 0～15 的编号；查询向量也使用同一套刻度。与 LVQ 每条向量单独定刻度不同，SQ4U 的刻度由训练数据共同确定，超出范围的值会截到边界。</p>
<p>教学例子：假设共享范围是 [0,3]，刻度间隔为 0.2。查询的一个坐标 0.34 编成 2，数据的一个坐标 0.72 编成 4；它们在这一维的整数距离贡献是 (2−4)²=4。所有维的贡献相加，就得到排序分数。因为所有点使用同一刻度，可以直接比较这些整数分数。</p>
<p>这算的是两端压缩后的近似距离。例子中两个坐标实际被当成了 0.4 和 0.8；乘上共同的间隔平方后，得到的是它们的平方距离，而不是原来 0.34 与 0.72 的精确距离。</p>
<h2>二、把邻居名单和压缩向量分别放到磁盘</h2>
<p>图放在 graph.pages，SQ4U 编码放在 sq4u_codes.pages；共享刻度、入口、候选池和查询页缓存留在内存。每次磁盘读取以 4 KiB 页为单位，一个页可以放多条记录。下图的“码行”就是一个向量的压缩编号。</p>
