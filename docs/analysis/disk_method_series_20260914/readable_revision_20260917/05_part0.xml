<p><b>先记住一句话：NSG 决定每个点应该连向谁，让搜索沿着少量有用的边前进。我们使用 Glass 库实现它，再用 SQ4U 压缩向量，最后把图和编码搬到 SSD。</b></p>
<p>本文先按“全流程 → 建图 → 存储与查询 → 误差”的顺序讲 NSG 原论文，再解释 Glass、SQ4U 和磁盘读取。图中的小例子只用于说明原理，不是实验结果。更新于 2026-09-17。</p>
<h1>第一部分：论文中的 NSG 怎么做</h1>
<h2>一、先分清 NSG、Glass、SQ4U</h2>
<table><thead><tr><th><p>名称</p></th><th><p>它是什么</p></th><th><p>负责什么</p></th></tr></thead><tbody><tr><td><p>NSG</p></td><td><p>论文提出的图方法</p></td><td><p>决定邻居连接，让查询少走弯路。</p></td></tr><tr><td><p>Glass</p></td><td><p>实现近邻搜索的程序库</p></td><td><p>提供建图、编码和搜索代码。</p></td></tr><tr><td><p>SQ4U</p></td><td><p>我们选择的 4 bit 编码方式</p></td><td><p>把向量压缩成短整数，用于计算近似距离。</p></td></tr></tbody></table>
<p>所以，Glass-NSG-SQ4U 是一个实现组合。<b>NSG 原论文的核心是怎样建图，SQ4U 的压缩步骤属于我们所选的库配置。</b>第一部分先用原始向量的欧氏距离解释 NSG。</p>
<p>这里“图”就是一份份邻居名单：每条数据是一个点，名单上的邻居对应它连出的边。查询从固定入口开始，计算邻居到查询的距离，优先继续搜索较近的候选。</p>
<p>读下图时按 1 到 6 看：前五步准备好图，最后一步才是用户发来查询后的搜索。下面逐步解释这些连接怎样选出来。</p>
