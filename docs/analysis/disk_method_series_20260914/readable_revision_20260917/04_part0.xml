<p><b>先记住一句话：OG-LVQ 把向量压缩后放在内存里，沿着图找邻居，用压缩后的向量计算距离。我们的磁盘版又把图和压缩向量搬到了 SSD，读取方式因此发生了变化。</b></p>
<p>本文仍分两部分：先按“全流程 → 编码 → 建图与查询 → 误差”的顺序解释原论文，再简述我们的磁盘改动。例子中的小向量只用于说明计算，不是实验数据。更新于 2026-09-17。</p>
<h1>第一部分：论文中的 OG-LVQ 怎么做</h1>
<h2>一、先看全流程：谁负责找路，谁负责算距离</h2>
<p>一条数据用一串数字表示，这串数字就是“向量”；用户这次要找的内容也表示成一个向量，叫“查询向量”。目标是找到离查询向量最近的若干条数据。</p>
<table><thead><tr><th><p>名称</p></th><th><p>在这里负责什么</p></th></tr></thead><tbody><tr><td><p>图</p></td><td><p>每条数据记住一小份邻居名单。搜索顺着这些连接走，避免把全库都算一遍。</p></td></tr><tr><td><p>LVQ</p></td><td><p>把向量中的小数变成短整数编号，减少存储和读取的数据量。</p></td></tr><tr><td><p>OG-LVQ</p></td><td><p>论文把优化后的图搜索与 LVQ 放在一起得到的完整方法。</p></td></tr><tr><td><p>候选池</p></td><td><p>这次查询暂时保留的一份近邻名单。里面的点未必都已经查看过邻居。</p></td></tr></tbody></table>
<p>一次查询的大致过程是：<b>从入口出发 → 计算邻居离查询有多远 → 保留较近的候选 → 继续查看候选的邻居 → 返回结果。</b>LVQ 决定这些距离怎样计算。</p>
<p>读下图时按编号 1 到 6 看：前四步是查询前的准备，后两步是查询时的工作。“残差”是压缩后丢掉的那一点数值差，第三节会用数字解释。</p>
