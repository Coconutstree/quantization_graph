<h2>16. 2026-09-17 代码修复与验证状态</h2><p>本次只修代码与文档、运行小型检查，没有启动全量实验。入口为 experiments/05_disk_system_fair/run_disk_suite.py，新协议 ID 为 disk_cgroup_v2_20260917。</p><table><thead><tr><th><p>问题</p></th><th><p>修复后行为</p></th></tr></thead><tbody><tr><td><p>声明 2 GiB、AS/RSS/对象求和混用</p></td><td><p>磁盘查询在 exec/加载索引前加入独立 cgroup v2；设置 memory.max、memory.swap.max=0，保存 peak/events/stat。另以 wait4 获取真实生命周期峰值 RSS；VmPeak 明确为采样诊断。</p></td></tr><tr><td><p>配置写死旧预算、缺少资源限制</p></td><td><p>主组默认 4 GiB、32 workers；`budget_scan` 显式选择 1/2/4/8 GiB，始终 32 workers；`thread_scaling` 单列，每个线程数独立 run-id。</p></td></tr><tr><td><p>验证集只用 1 worker、测试偷偷增加线程组</p></td><td><p>验证、调参与测试使用同一预算/线程/缓存配置；GIST 不再隐式添加其他条件。05A 测试使用独立 test 划分。</p></td></tr><tr><td><p>CPU/NUMA 口径不一致</p></td><td><p>从 export 阶段起显式登记 `--cpu-affinity` 与 `--numa-node`，在测量子进程执行 sched_setaffinity 和 numactl membind；OMP 使用目标 workers，MKL/OpenBLAS 设为 1。配置变动须另开 run-id。</p></td></tr><tr><td><p>缓存被强制等额或 C0</p></td><td><p>05A 固定 C0；完整系统默认 standard，保留原生缓存语义；C0 另列。取消新协议统一“缓存节点≤10%”验收，不替原生方法增加缓存算法。</p></td></tr><tr><td><p>只有 repeat=0、选择最快重复</p></td><td><p>正式五次重复，每点保存原始行和中位数；QPS、p95 提供 IQR、样本 CV。缺重复、混二进制/索引/查询划分时拒绝。</p></td></tr><tr><td><p>假 RSS、计时重复归因</p></td><td><p>外部实测 RSS 与 native_reported_peak_rss 分开保存。Glass/Symphony/OG-LVQ 的 Recall 计算移出 QPS 计时；distance/queue 未独立计量，写 null；整段遍历另报 traversal_wall_us。</p></td></tr><tr><td><p>筛选方法绕过绘图验收、旧结果覆盖</p></td><td><p>绘图核查协议、源 artifact、资源 sidecar、CSV 哈希与完整重复后才筛方法。发布到 `results/disk_environment/disk_cgroup_v2_20260917/&lt;run-id&gt;/`。</p></td></tr><tr><td><p>Ours 的内存下界阻塞其他方法</p></td><td><p>显式只选 AiSAQ 等其他方法时，不再用 Ours DB1 下界拒绝该方法；不自动宣称其可运行。</p></td></tr></tbody></table><p>原生 CLI 的 run_official_disk_baseline.py --execute 也要求有效 cgroup，默认搜索限额 4 GiB；建索引预算独立。该入口仍是诊断，formal_ready=false，不把进程总 wall time 当作搜索 QPS。</p><p>资源 sidecar 覆盖整个 native 进程生命周期；同一进程扫描多个 width 时各点共享生命周期峰值，不能标成逐点内存。已有端口的 memory_accounting_complete、cache protocol、算法等价验收要求没有被资源 wrapper 自动豁免。待验收或不支持的项保持 blocked/pending。</p><p>小型 Python 回归与 C++ 编译记录见 [修复记录](../analysis/disk_protocol_fix_20260917/README.md)。当前沙箱 cgroup 为只读，真实限额与 OOM 集成检查保留为待完成；没有可写委派目录或缺少 memory.peak 时入口拒绝执行，不回退为 RLIMIT_AS 或自报内存。</p>