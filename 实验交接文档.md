# 03/05 磁盘实验交接（2026-09-22）

> 下次新对话先读本文件，再读 `results/diagnostics/aligned_20260921/status.json` 和最新日志。本文是交接快照，实时状态以文件和宿主机进程为准。不要把旧状态中的 running 当成进程仍存活。
>
> **状态变更（2026-09-22）**：DiskANN baseline 已恢复成官方代码（pinned commit + 3 个声明式补丁），
> 03 端口已迁出 vendored 树，两侧编译选项已对齐；随后按用户决定**在同一个 TAG `aligned_20260921`
> 内就地覆盖重跑**——只重测二进制变过的 Ours-Disk 与 DiskANN-PQ-Disk，Glass-NSG/SymphonyQG 复用旧测量
> （最初试过新 TAG `aligned_20260922`，已废弃，半成品在
> `results/diagnostics/baseline_restore_20260922/superseded_aligned_20260922/`）。
> 进度：gist 03、agnews 03 已重测并出图；dbpedia 03 的 pair run 进行中；05 待跑。
>
> **先读 `DiskANN官方化交接.md`（自包含、可直接接手）**，再读本文件；本文件第 2/4/5 节中与旧
> 运行有关的细节已成为历史记录，实时状态以 `results/diagnostics/aligned_20260921/status.json`
> 与宿主机进程为准。

## 1. 用户目标与已授权范围

用户要求：修复 Ours / DiskANN 的协议与 I/O 批量读取问题，重跑 GIST，再跑 AGNews 和 DBpedia；每个数据集先 03，再 05；后台串行运行，完成后画论文 Recall–QPS 图。用户希望无需反复确认，遇到可修复工程问题直接处理。

最新要求还包括：写详细交接文档；修复 Glass 后接着跑；核查 Ours 为何比 DiskANN 慢。

- 03：共同 **4 GiB** 进程 RAM 预算，方法为 Ours-Disk、DiskANN-PQ-Disk、Glass-NSG-DiskPort、SymphonyQG-DiskPort。
- 05：**只有 Ours**，预算 **1/2/4/8 GiB**。不跑其他方法，不把旧 0.5 GiB 加回来。
- width 固定 **10/20/40/60/100/160/240/400/580**。
- 32 workers，NUMA node 0；CPU `0,4,8,...,124`；1 repeat；test 800 queries；warmup 100。
- Ours / DiskANN beam=4；Glass/Symphony 保留自己的原生搜索路径，不强行给它们加 beam=4。
- 不做 validation 性能扫参。Ours 只用 validation 查询采集热点；固定参数落盘后直接 test。
- 每次正式运行独立参考一致性、存储预处理和全进程峰值 RSS 验收，不能绕过准入。
- OG-LVQ 已被用户排除。AiSAQ/Starling 未完成正式准入，不应悄悄加入。
- 已有索引优先复用。只有明确不兼容的旧索引才显式重新 export，保留旧文件；不要在搜索期间隐式重建。

## 2. 当前进度快照

当前系列 TAG：`aligned_20260921`，不要与更早 `gist_w9_beam4_4g_20260921` 混淆。

| 数据集 | 03 | 05 |
| --- | --- | --- |
| GIST | 4 方法 × 9 档完成，36 行均准入；图表更新 | 1/2/4/8 GiB 各 9 档完成，36 行均准入；图表更新 |
| AGNews | Ours、DiskANN 各 9 档准入；Glass 旧索引不兼容后停止；2026-09-22 Glass 已显式重建完成，后台已恢复到 Glass 参考验收；Ours/DiskANN 日志确认复用 | 未开始 |
| DBpedia | 未开始；Glass 可能存在同样旧版本索引，队列已增加针对性自动修复 | 未开始 |

2026-09-22 恢复进程由工具会话 86079 启动（会话 ID 不是系统 PID）。不要据此推测当前 PID。

### 实时状态和日志

- 队列状态：`results/diagnostics/aligned_20260921/status.json`
- 总日志：`/tmp/aligned_03_05.log`（追加模式，包含先前失败；看末尾）
- 当前 AGNews 日志：`results/diagnostics/aligned_20260921/agnews_aligned_20260921_03_ram4/run.log`
- Glass 修复日志：同目录 `glass_repair/export.log`
- 原生验收：`results/diagnostics/aligned_20260921/admission_tests.log`
- 新旧二进制哈希：同目录 `binary_updates.json`

读状态之后，应检查当前原生子进程、最近日志和 artifact；不是每次都需要等待整轮结束。

## 3. 如何启动、恢复和处理失败

入口：`scripts/run_aligned_03_05.py`。

```bash
cd /home/kai3/coco/quantization_graph
python scripts/run_aligned_03_05.py >> /tmp/aligned_03_05.log 2>&1
```

在 Codex 工具里用 `exec_command` 的宿主机执行方式（`sandbox_permissions=require_escalated`），`yield_time_ms=1000` 返回后台会话，避免阻塞。普通沙箱 PID namespace 看不到宿主实验进程；用宿主 `ps` 检查。不要启动两个队列实例。

脚本现在支持断点恢复：读取已有状态、跳过已完成 job；未完成 run 复用既有已验收方法 artifact。完成某数据集整个 03 或四档 05 后，自动生成表格/图。状态用临时文件原子替换。索引/预算/官方参考等错误会停止队列并记录，不把失败当成功。

每个 job 的精确 CLI 保存在其 `command.json`。单独重跑一个 job 时，直接用该 JSON 的 argv 列表调用 subprocess，避免手抄参数或 shell 引号错误。

固定入口示例（实际优先用 command.json）：

```bash
python experiments/03_disk_system/run.py --datasets agnews \
  --methods Ours-Disk,DiskANN-PQ-Disk,Glass-NSG-DiskPort,SymphonyQG-DiskPort \
  --run-id agnews_aligned_20260921_03_ram4 --workers 32 --repeats 1 \
  --cpu-affinity 0,4,8,12,16,20,24,28,32,36,40,44,48,52,56,60,64,68,72,76,80,84,88,92,96,100,104,108,112,116,120,124 \
  --numa-node 0 --search-dram-budget-gib 4 --fixed-beam 4 --phase run --method-pipeline
```

PATH 需要 `work/tools/numactl/root/usr/bin`，队列已自动添加。

### Glass 本次失败及修复

`reusable_index` 检查几何、文件尺寸、输入 manifest hash、implementation fingerprint。AGNews 的 manifest hash 一致，旧 index.meta 的 fingerprint 是 `05c-glass-nsg-sq4u-r64-l400-native-odirect-v2`，当前要求 `05c-glass-nsg-sq4u-official-linear-pool-search-v3`，因此拒绝复用。

没有篡改 metadata 放行。`repair_glass` 只处理明确的 provenance/geometry 错误：

1. 原目录 `hybrid_disk` 重命名为 `hybrid_disk_before_aligned_repair`。
2. 从失败参考的 resources.json 读取原生 argv，改 phase=export，在原标准路径显式重建。
3. export 完成后，把失败的 `.reference` 目录移到 `glass_repair/failed_reference`，保留证据并释放 exclusive-create 路径。
4. 重新进入完整参考验收，再开始计时。Ours/DiskANN 已验收结果继续复用。
5. DBpedia 遇到同类错误也走此逻辑。其他错误不自动跳过。

如果 export 中断且 backup 已存在，脚本故意报错，不覆盖旧备份。应检查 `glass_repair/command.json`、export.log 和新目录完成程度，用保存的 export 命令恢复，然后移走失败参考，再恢复主队列。不要删除旧索引。

队列会按已保存 PID+命令核对，暂停已知 AiSAQ 构建，退出后恢复；不代表机器上任何新启动的后台任务都已暂停。性能异常时检查实际竞争负载。

## 4. 结果与可复用资产在哪里

运行目录：

- 03：`results/03_disk_system/<dataset>/<dataset>_aligned_20260921_03_ram4/`
- 05：`results/05_memory_budget/<dataset>/<dataset>_aligned_20260921_05_ram<B>/`
- B 为 1、2、4、8。

每轮含：`raw/<method>/test/` 原生 JSON、query trace、资源证据、参考目录；`manifests/fixed_parameters.lock.json`；`tables/formal_test_rows.csv` 与 manifest。

论文输出（每次完整实验完成后更新）：

- `results/03_disk_system/<dataset>/tables/recall_qps_summary.{md,csv}`
- `results/03_disk_system/<dataset>/figures/system_qps_recall.{pdf,svg,png,tiff}`
- `results/05_memory_budget/<dataset>/tables/recall_qps_summary.{md,csv}`
- `results/05_memory_budget/<dataset>/figures/memory_qps_recall.{pdf,svg,png,tiff}`

源 CSV、图注、provenance、字体检查在图目录。新图仅在完整实验结束后替换；旧输出备份在 `results/diagnostics/aligned_20260921/previous_outputs/`。05 完整图也备份到最后一个预算 run 的 figures；每个预算原始数据仍在各自目录。

索引根：`work/05_disk_system_fair/disk_root/05_disk_system_fair/05C_disk_system_fair/<dataset>/<method>/hybrid_disk/`。

查询 split：`artifacts/query_splits/<dataset>/shared/`。输入 manifest：`results/manifests/<run_id>/inputs/05c_<dataset>.json`。

## 5. 本轮已修改的代码及验证

### Ours frontier 邻接预取

`experiments/02_disk_shared_graph/native/src/ours_port.rs`：选出 beam 个 frontier 后，用 `graph.read_nodes(&frontier)` 预取邻接表，仍按原顺序处理每个节点的筛选/距离计算/候选插入；locality 特殊路径保留原处理。没有把不同扩展节点的 gate 合并到同一旧阈值。

### DiskANN 隐藏 cap

`experiments/03_disk_system/native_diskann/src/main.rs`：两个搜索器构造（direct 和内部参考）的第二参数从 128 改为 `usize::MAX`。这个参数是 search_io_limit，不是并发数。限制的是遍历加载节点计数，不含全部重排物理读请求。

新版 GIST DiskANN Recall 不再在 97.7125% 平台停住，width=580 达到 99.7625%；不能把旧平台数据用于当前结论。

### 编译及验收

```bash
cargo build --release --manifest-path src/graph_core/Cargo.toml --bin qgraph05_shared_graph_port
cargo build --release --manifest-path baselines/diskann/Cargo.toml -p diskann-disk --bin qgraph05_diskann_port
OURS_ALIGNMENT_BASELINE=/home/kai3/coco/quantization_graph/work/alignment_20260921/ours_before \
  python -m unittest discover -s src/disk_bench/tests -p test_formal_admission.py
```

5 项通过；Ours 完整 1-bit/PCA/fixed beam 对旧二进制的 result IDs 和已有搜索计数字段一致；新旧 profile 各自生成，不能拿新 binary hash 绑定的 profile 给旧 binary。小数据通过不替代真实数据验收。

编译后 `src/disk_bench/ports.local.json` 中涉及两个二进制的 binary_sha256 已更新。以后改原生代码必须重新编译、更新 registry hash，用新 run ID，不覆盖当前测量。

### 后台与制图代码

- `scripts/run_aligned_03_05.py`：串行调度、恢复、Glass 修复、自动图表。
- `scripts/summarize_gist_formal_tables.py`：名字含 gist，但 render 支持 dataset 参数。
- `scripts/plot_gist_formal_recall_qps.py`：名字含 gist，但 draw 支持 dataset 参数、数据自适应坐标范围。
- `src/disk_bench/tests/test_formal_admission.py`：可选旧二进制对照。

制图用 nature-figure 技能，Python 默认已保存。字体 ≥7 pt，89×78 mm，PDF/SVG 可编辑，TIFF 600 dpi，PNG 300 dpi。所有九档点保留、无平滑、无 Pareto 过滤；03 log QPS，05 linear QPS。每次只有一次测量，不造误差条。后台自动检查不替代人工看图，当前新图视觉验收仍单独标记 pending。

## 6. Ours 慢的核查结论与待办

详见 `results/diagnostics/aligned_20260921/ours_diskann_performance_audit.md`。

- 不应笼统说所有 Recall 下 Ours 都慢：最新 GIST 中高 Recall 段 Ours 更快，但最高 Recall 低于 DiskANN。
- AGNews 已完成的两个方法显示明显 Ours 劣势，不能只归因于读请求数量。
- Ours 的 io_wait_us 包括读路径部分软件开销；DiskANN 的 distance_compute_us 实际映射官方 cpu_time_us。不能直接把这些字段当同口径纯硬件 I/O 与纯距离核耗时。
- Ours C++ DirectAioReader 使用 libaio；DiskANN Linux reader 使用 io_uring。相同 O_DIRECT 不等于相同后端/队列行为。
- 两边布局、候选物化与重排不同。Ours 在候选插入前读取通过 gate 的记录；DiskANN 用驻留 PQ 导航。Ours 最多重排 100 个候选，限制高 Recall 的可能性尚待消融验证。
- 存储预处理明确 `device_cache_controlled=false`、`cold_storage_claim=false`；O_DIRECT 不绕过硬盘/RAID 缓存。单次固定顺序测量不足以把差异全部归因到算法。
- 下一步只做独立诊断、不要在后台正式实验运行时修改二进制或运行竞争 I/O benchmark。可在正式队列结束后做固定页访问 trace 的 libaio/io_uring 对照、交错重复测量、缓存/候选记录等待分解。

## 7. 环境和容易踩的坑

- 仓库 `/home/kai3/coco/quantization_graph`，shell bash，Python miniconda3。
- 文件系统只允许仓库和 /tmp 写入；numactl/O_DIRECT/宿主进程操作需要宿主执行。
- 本机存储此前识别为旋转盘/硬件 RAID，不能写成已确认 NVMe SSD。
- `pytest`、`psutil` 曾不可用；用 unittest 和 /proc。/proc stat 的 comm 可含空格，不能直接按整行 split。
- repo 有大量已有未提交改动；不要 git reset/checkout 清空。
- `--phase run --method-pipeline --fixed-beam 4` 才是当前固定参数流程；不要恢复旧 validation beam 搜索。
- 4 GiB 是全进程预算上限，不要求实际 RSS 恰好 4 GiB；当前 RSS admission 不是 cgroup 硬限额。
- 1 GiB GIST 仍能容纳完整 960 维 1-bit；1/2/4/8 曲线不能当 PCA 优势证据。
- 旧 0.5 GiB 曾 wait4 RSS ~3.68 GiB，而采样 RSS ~479 MiB，差异未彻底解释；已排除在当前 grid 外，不篡改旧证据。

## 8. 下次对话优先操作

1. 读本文件和实时 status；查日志末尾及宿主实际进程。
2. 如已停止，找真正错误（忽略日志中旧失败），保留失败证据、修复后恢复同一队列。
3. 确认已完成方法 artifact 被复用，未重复跑 GIST。
4. 实验完成后核对每个 03=36 行、每个 05=4×9 行及各自 formal_ready，再检查新 PDF/PNG，更新视觉 QA。
5. 继续性能诊断时明确已证实与推测，不为美化曲线偷偷改变参数或移除慢点。

## 9. Validation、热点缓存与 PCA residual 问答（2026-09-22）

### Q1：Ours 的 validation 查询向量是验证集的一部分吗？GIST 划分多少查询做 validation？

是。validation 和 test 是同一查询集合中的两个固定 split，不能混用。当前 GIST 的
`artifacts/query_splits/gist/shared/validation_query.fvecs` 含 **200 条查询**，对应
`validation_gt.ivecs`；`test_query.fvecs` 含 **800 条查询**，对应 `test_gt.ivecs`。
本轮 test 的 800 条查询不参与参数或热点选择。查询文件大小也可交叉核验：GIST 维度为
960，validation 文件大小为 `200 × 960 × 4` 字节，test 文件大小为
`800 × 960 × 4` 字节。

### Q2：Hybrid 的热点是用 validation 验证出来的吗？

是。`validation_hot_then_dynamic_fifo16_v1` 会在正式计时前单独读取 validation 查询，按
各 width/beam 的访问次数归一化并合并，生成静态热点排名和 hot-profile manifest；test
阶段只复用已冻结的 profile，不用 test 查询训练热点。热点 profile 的哈希会写入 validation
结果和 tuning lock，binary、路由、预算或索引变化后必须重新生成匹配的 validation profile。
因此这里的“validation”是访问行为的冻结/选择阶段，不是用 test 查询反向调参。

### Q3：PCA 以后会不会多出一份 PCA residual，用于精排？

不会。当前 Ours 的 PCA 分支使用 PCA 投影后的 **1-bit 表示做导航和 shortlist**；代码策略名
明确为 `pca1bit_m32_no_residual`，这里的 no-residual 指不额外加载或保存 PCA residual
norm。进入 shortlist 后，会读取完整维度的 4-bit 表示重算距离，随后沿用原有的
**residual4 sidecar 精排**。因此流程是：

```text
PCA + 1-bit 导航 → shortlist → 完整维度 4-bit 重算 → 既有 residual4 精排
```

PCA residual 与既有 residual4 不是同一个东西。当前索引仍有 `ours_full4_residual.pages`，
记录缓存也同时缓存 compact 4-bit 与 residual 重排数据；没有新增一份“PCA residual”用于
精排。相关实现和字段见 `src/disk_bench/ours_pca.py`、
`experiments/02_disk_shared_graph/native/src/main.rs` 中的 `full4_candidates`、
`rerank_candidates` 和 residual layout 检查。
